[package]
name = "yuanyuan-protocol"
version = "0.1.0"
description = "Versioned local task-watch protocol for Yuanyuan"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
zeroize = "1"
