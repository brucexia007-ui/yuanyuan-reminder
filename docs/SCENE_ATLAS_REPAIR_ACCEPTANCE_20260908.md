# 情境图集衔接修复续验（2026-09-08）

22:11 程序续验更新：用户反馈已收到，原 Sandbox 已保留数据安装 1.5.22，并完成两次拖动、普通点击与正常重开位置恢复的针对性回归。正式图集和本页候选均未替换；下面 21:18 等记录为历史状态，不再以“未安装程序/待拖动反馈”描述当前阻挡。素材仍须实际播放及系统缩放验收，程序回归不替代素材通过。

状态（21:18 更新）：工具回归和 18 行独立静态复核通过；真实播放与安装版验收未完成。候选只在忽略工作目录，未替换正式素材。本素材阶段基于 1.5.19；当前程序候选为 1.5.22 development，自动门禁通过、尚未安装，原 Sandbox 安装版为 1.5.21。这些程序门禁不为未安装的新图集背书。21:00 的最近一次桌面观察为正常桌面、宠物夜间睡姿，没有新锁屏观察；当前等待用户真实鼠标拖动反馈，未发送输入或安装、重启应用。完整 E01–E30 状态见 [我的宠物验收矩阵](MY_PET_E2E_ACCEPTANCE_V1.md)。

21:18 续验：复核下列独立静态报告及四处播放告警，未发现足以仅凭静态证据启动新一轮生成的硬失败。按 hatch-pet 的视觉验收要求，静态截图、数值差异和演示 GIF 文件存在均不等于已观察动态播放；保留现有候选，等待可交互桌面的真实播放与系统缩放验收。本轮没有生成图片、重装配图集、修改正式资源，也没有把任何动态或安装版项目改为通过。

## 修复内容与原始失败

上一轮已确认并修正先八等分再提取前景造成的身体截断，但夜间/工作循环仍明显放大，且工作循环衣服与眼镜不一致。进一步检查发现每行独立适配最大格高会把蜷睡和低伏循环放大；需要同时修正动作与注册方法，不能仅反复修改生成提示。

`build_scene_atlas.py` 新增显式端点参考，以指定源列匹配语义一致的相邻端点高度、水平中心与落地基线。全部原始姿态使用同一缩放、只重采样一次；无法装入时直接拒绝，不裁边、不悄悄改小。`--row-registration` 支持相对路径 JSON，拒绝非法行、空引用、非法列和不合格参考格。格式见 [宠物包规范](PET_PACK_SPEC.md)。

按 hatch-pet／imagegen 流程，仅生成夜间循环和工作疲劳循环两个完整八帧行，附带既有猫身份、进入末姿态、退出/恢复首姿态及布局参考。源图实际为 RGB 洋红背景，不称为原生透明；已移除已知背景，每个新归一化行仅执行一次保留 alpha 的边缘去色。完整原稿及提示保存在 `work/scene-repair-20260908/continuity/`。

## 自动与独立静态证据

- 新注册 API 在旧函数上测试先失败；实现后 19 项通过，再补配置边界后 **22 项通过**。最终日志 `registration-tests-final.log` SHA-256：`69FDACE7FF2F4379FD2255BA9B1593E1FECDB957080092C144A8744BA3C4079E`。
- 候选 `continuity/candidate/scene-atlas.webp` 为 4,443,588 字节，SHA-256：`D435F7C0C12F41B3F744192B1CB377C9D14C3217D3B957174DF61ECFB82EA83D`。
- 验证 JSON SHA-256：`792B22478E5D8C72F982049D3F57BBB47C7B1E7FBBF7924E6066B0F3B9D5EA38`。1536×3744、144 格边距、隐藏 RGB、实际 WebP 解码和精确 RGBA 验证通过。
- 重新解码确认只替换两行，其余 **16 行全部 RGBA 像素逐字节保留**，新两行 alpha 与去色前一致。证据为 `candidate/independent-deterministic-verification.json`。
- CLI 首次因输入文件名含 `-key-removed` 后缀而不是约定动作名，读取失败、未写输出；建立哈希验证过的正确命名输入后成功重跑，两行像素与候选去色前一致。该产物仅用于复核工具，不另计视觉通过。
- 独立复核先覆盖夜间/工作，再覆盖其余动作组：**18/18 行静态无硬失败**。两个循环的放大和工作服饰/眼镜漂移已消除；逐组结论与告警存于 `candidate/independent-full-static-qa.json`。静态通过不证明动画完整播放或系统缩放通过。

## 仍需实际播放判断的告警

- 夜间循环第 3/5 列比邻帧低约 5–8 像素，看起来是低头，但节奏仍须播放判断。
- 工作疲劳循环第 6→7 列抬头约 11 像素，需观察是否突兀。
- 热身提醒末帧→循环首帧有较大手臂姿态变化。
- 学习专注与好奇边界存在书本道具消失/出现；需结合真实学习互动确认是否产生明显跳变。

这些告警未被改写成动态通过，也没有仅凭数值差异立即重做已经结构合格的行。下一步是查看真实清单时序与 Windows 播放，再决定是否需要有针对性的完整行修复。

## 预览、版本与数据边界

`candidate/manifest-timed-previews/` 包含夜间（40 帧 / 11,520 ms）及工作（输入 57 帧 / 编码 56 帧 / 18,840 ms）衔接 GIF，绑定图集和当前动画清单。工作末帧与稳定帧相同，编码合并后解码总时长仍一致。三次循环与 3 秒静态保持属于演示夹具，不是实际后台期限或应用录屏。旧的统一 280 ms 预览继续保留为历史诊断，不混作真实清单时序。

两份生成原图已精确复制并核对 SHA-256；仅清理生成缓存中的重复文件及空目录，工作原稿和候选仍可恢复。没有修改正式图集、清单、用户数据或安装包。正式图集仍为 `8BA729D70B100E167073ADF1566C9E2B0B37A578CD1CF32EB1BCAF76622A7170`。

本阶段仅验证素材工具和候选静态，不以此前 1.5.19 的全量前端/Rust/生产构建为新素材背书。正式替换之后仍须递增统一补丁版本、重跑完整门禁并绑定新安装版关键回归，全部验收未完成前不发布。

08:20:24（UTC+8）通过 computer-use 重新列出并精确选择当前 Sandbox，仅获取截图，确认“启用来自必应的建议”隐私请求仍遮挡昵称区域。未输入、未点击、未激活其他窗口或重启；记录于 `continuity/desktop-blocker-recheck.json`。同一环境阻挡已跨三轮持续；静态修复及安全检查已经推进到实际播放门禁，后续必须等待用户手动处理提示。验收没有完成，不将目标标记为成功。

11:41 续验更新：用户处理后已确认上述隐私提示消失，同一 1.5.19 安装进程继续昵称验收。11:53 桌面输入及窗口恢复先后返回拒绝访问，按 computer-use 停止输入，详见 [E2E 续验记录](MY_PET_E2E_ACCEPTANCE_V1.md)。这是新的环境问题，原因尚未确定；本次没有安装新图集，也没有完成其动态或 Windows 播放验收。
