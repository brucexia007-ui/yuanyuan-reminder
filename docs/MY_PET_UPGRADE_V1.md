# “我的宠物” V1 实施与验收记录

2026-09-09 11:25 续验：1.5.23 的 F32 原失败路径已通过安装版复测。学习第一题反馈保持展开时，鼠标点击重叠文件选择器“打开”正常，No Learning 能力/许可准确，确认加入但不自动切换，7 个文件与原 ZIP 相同，Full 的 8 个文件未变、K 保留。自然补水中断后恢复同一会话到第二题，数字键 3 作答、Ctrl+End 定位列表有效；两次新作答/复习各只记一次。换包按钮遇激活失败，一次重新绑定取景成功但重试输入仍失败，按 computer-use 规则停止。后续只读记录活动提醒将会话暂停在 2/3；F31 结算恢复尚未跑到，缺学习形象未成功切入。980 份快照严格匹配 13 次预期变化，审计 `upgrade-1.5.23/import-mouse-retest-audit.json` SHA-256 `8F871A150A05CA7F78DA24FC49130FBF2237E47EC3895183EE26A33B073AB1A8`，完整路径见矩阵。无安装、重启、产品修改或版本递增，原 E01–E30 与全部扩展范围未整体放行。

2026-09-09 10:00 续验：用户确认真实 T 输入正常，鼠标重新打开学习页并滚动成功，但“开始一轮”操作及一次窗口恢复又遭 `GetCursorPos / 0x80070005`，按 computer-use 规则停止原生输入。256 份新增快照中只新增本轮自动输入前已经保存的一条 T 提醒，其余全部已采集字段不变，未产生新学习会话；没有重复保存、安装、重启或产品改动。只读确认原 1.5.23 进程 14988 仍响应、宠物与面板实际为 150% DPI。审计 `upgrade-1.5.23/resumed-input-audit.json` SHA-256 `2D391301CC68BB7F2842129A00C62AB8ADBDD8E2DCE683271174403CB8CC77B9`，完整路径见矩阵。F31/F32 仍待安装版复测，键盘、DPI 视觉及 E01–E30/扩展范围没有缩减或整体放行；当前是桌面操作通道阻塞，不是缺少用户授权。

2026-09-09 09:38 当前状态：修复候选及原沙盒安装版均为 1.5.23，未发布。综合校验、Rust 工作区全量/无学习测试、两种生产构建通过（前端 330 项 + 包工具 5 项，主库 339 / 243 项、各 1 项原有忽略）；正常退出旧版后保留数据升级，239 个冷归档文件与安装前后哈希一致。新进程 14988 正常显示 Full/K 和今日页，332 份跨升级快照核对所有已采集业务字段不变。安装审计 SHA-256 `2B2853A8EA9B713AF49CFAD1DD0B0695773B5320AF2810A41E7858735ADA5DB9`，完整路径见端到端矩阵。键盘输入未转发，随后桌面工具激活失败并在恢复时返回 `GetCursorPos / 0x80070005`，已停止原生输入，待用户确认解锁及沙盒真实键盘可用；不是缺少范围授权。F31/F32 新安装版复测未完成，无学习版尚未安装，原 E01–E30、素材动态、DPI/键盘/读屏和异常测试全部保留。

2026-09-09 09:16 续验：当前源码修正候选 1.5.23，沙盒仍运行原 1.5.22，未重装/清数据。已取得窗口真实 144 DPI（150%）；在系统减少动态下完成六卡导入及三题学习、Full/Basic 反馈中切换，答题/复习各记 3 次。新增 F31（提醒后结算页丢失、学习装扮残留）与 F32（学习透明窗口拦截文件选择器“打开”）均未计为通过。261 份快照按每次实际操作精确核对，原有其他业务字段不变，审计 `resume-20260909/learning-reminder-audit.json` SHA-256 `2458BFA2891F2C9EE99B9E8411BF93A2DA0DD7663CA296C9DB07F7CB10B5ABE2`。F31 组件旧实现 3 项失败、修正后所在文件 48 项通过，后端仲裁 19 项通过；F32 生命周期层级修正已补回归，完整门禁和安装版复测待完成。全矩阵、异常分支、真实 100%/125% 与读屏等要求不缩减。

2026-09-09 08:46 续验：用户的完整 E2E 授权已接续到原沙盒，未重建数据或更换安装版。E09 非空分支已执行：清除 R、恢复圆圆，Full/K 保留，双向普通切换核对通过。171 份连续快照仅有一次重置和两次切换，其余全部已采集业务数据不变；原进程 12308 保持。审计 `resume-20260909/default-reset-audit.json` SHA-256 `453A32417A58C5AF20BA617E9880AFB7377D9906983FDF5A24BE4429BC8B65F7`（完整路径见端到端矩阵）。当前继续系统减少动态与学习组合；真实 DPI 尚未取得，沙盒没有可用显示缩放入口，不改宿主设置，不用页面缩放替代。版本仍为 1.5.22，未新增产品改动，全部矩阵未完成，不发布。下方 09-08 的等待确认停点为历史记录，不是当前状态。

23:28 当前停点：E09 非空昵称分支前置完成，内置圆圆昵称 R、当前 Full 昵称 K，恢复默认按钮可见但未点击；等待清除 R、切回圆圆且保留 Full/K 的操作时确认。111 份连续快照确认只有两次昵称保存与一次形象切换，其余已采集数据不变，首次错误粘贴草稿未保存。审计 `upgrade-1.5.22/default-nickname-preparation-audit.json` SHA-256 `AA436BBAB738CBBC8F9DF602974C3A9EE1397677ACB08982B5001FAD30BB7981`（完整路径见端到端矩阵）。这不是 E09 非空分支通过，完整验收仍未放行。

23:20 续验：经用户操作时确认，已移除闲置 E2E No Scene 的本地副本及昵称 y，原始包文件保留，可重新导入；Full 选择及八个素材文件哈希不变。随后在昵称映射为空时恢复默认，内置圆圆成为当前形象、原生菜单恢复圆圆，Full 包仍在。271 份连续快照仅有这两次预期变化，其余全部已采集业务数据不变；实际进程 12308 未重启。审计 `upgrade-1.5.22/pack-removal-default-empty-audit.json` SHA-256 `EAAD312FA2D5F89F4508ED43FD3DE9CB1CBAF65229784B576C35A267BA4F9D5B`（完整路径见端到端矩阵）。E08 正常删除通过，E09 仅无昵称分支通过；既有圆圆昵称清除与其他昵称保留尚未实测。没有新产品改动或正式素材变更，全部矩阵仍未完成。

基线：圆圆 1.5.8 工作区；首轮候选版本：1.5.9；当前修复候选及沙盒安装版：1.5.23（学习结算恢复/原生导入遮挡修正，自动门禁及保留数据安装通过，真实路径复测尚未完成）；日期：2026-09-09。保留原有未提交情境装扮等改动，不合并其他产品的品牌、标识或数据目录。主数据库继续使用 schema 13。

22:55 续验：经用户授权，“使用原名”已清除当前 Full 测试昵称 x，页面、原生菜单和设置称呼恢复 E2E Full；历史正文不变。Full→No Scene→Full 后分别使用 y、E2E Full，511 份连续快照仅有一次昵称清除和两次选择变化，其他已采集数据均保持。证据 `upgrade-1.5.22/original-name-audit.json` SHA-256 `40FACF1940594580268D5BF60EE3B615D7298DFF63C59AAC0D886362B8D4EED0`（完整路径见端到端矩阵）。当前包/内置包保护复核通过；停在闲置 E2E No Scene 的移除确认框，尚未删除本地副本或昵称 y，待操作时确认。无新源码改动、安装或重启，原完整矩阵与视觉/DPI/读屏范围不缩减。

22:11 续验：用户确认旧版真实鼠标可移动后，正常退出并保留数据安装 1.5.22，安装退出 0、实际字节匹配。230 个数据文件的冷归档及安装前后哈希一致；两次工具拖动成功，普通点击仍打开今日页，正常退出重开后的新坐标 1557/1232 和全部已采集业务字段保持。77 份连续快照与进程身份交叉核对；证据 `upgrade-1.5.22/installed-drag-audit.json` SHA-256 `E79F5B0ABED984D01F1318BD83B5E88E8A1AE05669DE3AC706AF56C457F426B4`，完整路径见端到端矩阵。没有强制结束应用、重建合成数据或改动正式图集；极快/取消手势、多窗口冲突、其他 E01–E30 及动态/DPI/读屏仍待完成，不发布。

21:09 续验：保留数据更新脚本的默认只读预检通过，原进程 7100 与只读记录器 6900 保持运行，全部已采集业务字段与既有基线相同；232 个文件仅做归档前清点，未复制/更改。脚本的中文路径编码问题已经修正并保留失败证据，没有修改产品源码或安装任何候选。21:00 只读桌面观察正常、宠物夜间睡姿，没有新锁屏；仍未发送鼠标/键盘输入。预检审计 `work/my-pet-e2e/upgrade-preflight-1.5.22-audit.json` SHA-256 `D8F124186592981B61D0F73156EFB5C3D3A0A20EC2C2272240AC3B39EC834741`；真正更新和完整原矩阵仍待完成，先等待用户真实拖动反馈。

20:49 门禁完成：1.5.22 前端 323 项、5 项包工具测试、综合校验、Rust 工作区全量/无学习测试及两种生产构建通过；主库分别 333 / 241 项通过，各 1 项原有忽略。候选指纹 `work/my-pet-e2e/candidate-1.5.22-gates.json` SHA-256 `47A96E0C98B96298E10DC8FDF065C8A33FCE2B0BA5BA34E70CB316AA4E6F265E`，两个二进制更新输入已准备并核对，没有安装或重启当前沙盒。原 1.5.21 新增 375 份只读快照中的已采集业务字段/坐标全部不变；真实鼠标反馈仍待用户确认，安装版拖动回归和完整 E01–E30 未放行。首次启动脚本异常及 UTF-16 日志审计修正保留记录；仅清理 8 个旧测试 PDB，未删除源码、安装包、数据或验收证据，详见端到端矩阵。

20:32 续验：只读取景确认原沙盒桌面已解锁，保持安装版和合成数据；没有继续鼠标/键盘输入。快速拖动在异步几何读取期间丢失的问题经 4 项失败回归复现，修正为同步记录手势、顺序处理几何/移动/保存；新增 15 项、所在文件 41 项组件测试通过。源码按补丁策略同步为 1.5.22，尚待完整门禁和新安装版复测；不将本地复现当作原生手势异常的唯一原因，也不将模拟缩放当作真实 Windows DPI。1.5.21 冻结证据保留，完整 E01–E30 不放行。

20:17 针对性回归通过：真实 25 分钟专注自然结束后，学习邀请写入同一 ID 的 `candidate` 与 `suppressed/manual_only`，预约的 20:09 提醒只生成一次并完成，学习首页继续响应。正常菜单退出后重开已安装程序，Full 选择、独立 `x/y` 昵称、三条完成历史及全部已采集业务字段保留。429 份快照、15 次字段变化、32 条原生观察与安装/源码指纹绑定审计通过，证据 `installed-audit-after-restart.json` SHA-256 `6B99853E46CF487094D39697F57EB7AF7709AED4258B90809BE7FD11B6EBC02A`（完整路径见端到端矩阵）。自动拖动被表现为点击，真实新位置持久化尚未通过，已请求一次人工鼠标复核；正式情境图集缺陷和完整 E01–E30 仍未全部完成，不发布稳定版。

20:04 安装版续验：同一沙盒已归档旧程序及数据后保留数据更新至 1.5.21，实际安装指纹吻合，13 个安装前后数据/素材文件及冷归档哈希独立核对一致。旧到期提醒恢复并完成、学习首页可用，缺装扮包预览/导入不自动切换、专注中双向切换、独立 `x/y` 昵称和原文历史已观察。正在等待真实 25 分钟专注自然结束及其后的 20:09 提醒，尚不把死锁安装回归记为通过；位置重启持久化和完整矩阵继续保留。279 份连续快照的本轮证据见 `installed-audit-before-finish.json`（完整路径与指纹在端到端矩阵）。

19:31 门禁完成：1.5.21 综合校验、Rust 工作区全量/无学习测试及两种 Tauri 生产构建均退出 0。主库分别 333 / 241 项通过，各有 1 项原有忽略；前端 308 项及 5 项包工具测试通过。候选指纹归档 `work/my-pet-e2e/candidate-1.5.21-gates.json` SHA-256 `6A87BE20A09148AFA8D09799F0F92BF2F026FCC3F5483D2D7A93921B35FFB956`。19:26 沙盒取景返回其他窗口内容，重绑一次仍错位，已停止原生输入；新候选未安装，原现场未清理，完整矩阵不放行。

19:22 续验：用户解锁后，19:12 历史页响应正常；只读诊断确认 16:20 提醒正确存为 `2026-09-08T08:20:00+00:00`、未触发且主库完整性正常，19:13–19:14 学习页持续加载。提取原抑制分支的有界回归超时，确认重复锁定学习运行时的源码缺陷；一次加锁完成评估与记录后，14 项学习运行时测试通过（含 3 项新增）。原进程未重启，未清数据；1.5.21 开始全量门禁，后续必须验证专注结束后提醒仍能到期及学习页可继续访问。

17:13 门禁续验：1.5.20 综合校验、Rust 工作区全量和无学习测试、两种 Tauri 生产构建全部通过；主库分别为 330 / 241 项通过，各有 1 项原有忽略。辅助程序 2 项边界测试和重建通过，候选指纹报告 `work/my-pet-e2e/candidate-1.5.20-gates.json` 绑定实际构建、源码、日志及功能配置。首次主动中止/真实写满失败及旧缓存清理记录均保留。未启动新候选，也未关闭或重新初始化当前沙盒；桌面仍待用户手动恢复后实测，不发布。

16:52 续验：1.5.19 安装版完成原生 Unicode 24/25 标量边界、TAB 拒绝、单表情接受及改名后含“圆圆”的提醒正文保护（列表/数据库部分）。随后发现拖动后疑似保存旧坐标；本地组件回归确定存在异步竞态。1.5.20 候选串行化原生移动和最终保存，8 项新增回归及 308 项前端测试通过，正在重跑综合/Rust/生产构建门禁。原沙盒仍运行 1.5.19，没有重装、重启或清数据；16:48 新观察为 Windows 锁屏，按 computer-use 停止桌面输入。预约通知、位置实机复测、全部 E01–E30 和图集动态/缩放仍未完成。

以下保留先前候选的阶段记录，不代表 1.5.20 已完成验收：

最新进展：1.5.18 原生复测补充了专注中 Full→No Scene→Full、真实 24/30 分钟阈值、补水和用餐分别到期/完成。另发现球互动结束后注视帧持续遮住工作装扮；1.5.19 清除该残留，4 项新回归先失败后通过。前端 300 项、综合校验重跑、Rust 主库 330 项及无学习主库 241 项（各 1 项原有忽略）、两种生产构建通过；1.5.19 新安装版正常工作阶段注视恢复的针对性原生复测通过。07:01 昵称输入触发 Windows 输入法隐私请求，未保存，按 computer-use 规则暂停桌面操作，须用户自行处理；完整矩阵未完成。疲劳循环素材的部分帧右侧断边也已确认，本候选未修改素材，仍阻止视觉放行。旧失败保留，完整矩阵见下方端到端记录，尚不发布。

历史阶段（1.5.14）：增加原始 ZIP 中央目录检查，并使失败提示立即进入可视区和可访问焦点。前端 254 项、综合校验、Rust 工作区全量及无学习主库测试、两种生产构建均通过；完整安装版已在断网合成数据沙盒核对指纹。十四类异常包全部被拒绝，完整/基础包正常预览后取消，暂存清空，全部已取证业务字段与基线一致；基础包睡眠预览不改变桌面提醒。1.5.13 重复文件名包错误进入预览的失败记录保留。实际恢复、业务中切换、无学习安装版、系统缩放及辅助功能等仍未全部验收，不发布稳定版。

23:50–23:53 补充：1.5.14 完整包预览时受控中断测试应用，重启清空未提交暂存，昵称/选择及全部已取证业务字段与基线相同；同包重试生成新令牌并成功加入，不自动切换，E21 通过。随后中文昵称输入遇 Windows 输入法“启用来自必应的建议”隐私提示，按 computer-use 规则暂停界面操作，未保存昵称、未操作隐私选择，等待用户手动处理；这不是应用校验失败。无学习 1.5.14 新输入已冻结但未启动或安装，不能计作 E25 通过。

后续真实安装版端到端验收已开始，完整矩阵和逐轮证据见 [MY_PET_E2E_ACCEPTANCE_V1.md](MY_PET_E2E_ACCEPTANCE_V1.md)。1.5.9 实测发现原生“我的宠物”路由白名单遗漏，已在 1.5.10 修复并实机确认；1.5.10 的学习导入窗口昵称遗漏在 1.5.11 修复。1.5.11 备份恢复未等待异步确认，1.5.12 虽阻止错误覆盖，但原生确认兼容层调用未注册接口而被拒绝。1.5.13 改为实际注册的消息接口及匹配权限，前端 253 项、综合校验、Rust 工作区与无学习主库测试、两种生产构建通过；首次综合校验因磁盘不足失败，清理四个本轮旧构建缓存后顺序重跑通过，保留失败日志。1.5.13 已于 22:32 离线安装并严格核对指纹，22:35–22:36 原生恢复确认框打开、等待及取消通过，三个阶段只读快照除时间外完全相同。实际确认恢复、其他破坏性流程与无学习安装版仍待实测。四类包、独立昵称、进程重启和学习三题计数已有旧候选实证，不等同全部验收完成。以下 1.5.9 结果保留为历史阶段记录，不冒充新候选的全量实机通过。

## 已实现

- “设置 → 我的宠物”子页面、宠物右键同名入口；独立昵称、原名恢复、动作预览、导入确认、切换、闲置包移除和恢复默认。
- `AppSettings.petProfile` 保存 schemaVersion、selectedPackId、nicknames。普通设置及窗口位置保存保留最新宠物字段，专用修改命令携带后端修订号。
- Rust `PetStore` 维护安装目录、一次性暂存、内容地址去重、启动扫描、损坏后重新导入修复和运行期图片完整性；图片只能通过内部 ID 白名单读取，不新增网络服务或日常网络依赖。
- 前端先订阅再读取快照；异步准备期间保留原画面，忽略旧修订和过期加载。按实际宠物生成缺省动作，不与圆圆图集混合。
- 短互动切换时释放展示租约、移除玩具；提醒、专注、工作疲劳、学习答案与休息期限不重建。答题反馈在无动画、缺学习资源或切换时由界面完成，不等待失效回调。
- 固定宠物文案通过昵称模板生成；产品名、Windows 标识、原始提醒正文、任务摘要、学习内容和历史不替换。
- 备份包含昵称及选择，不包含素材；缺包保留原引用与昵称，临时使用内置圆圆。重新导入原内容后恢复选择。
- 格式规范、离线打包工具、使用说明、变更记录、素材许可及生产依赖许可归档同步更新。默认分发资源未新增第三方形象。
- 破坏性操作的确认兼容桌面对话框异步返回：仅明确同意才执行，待决/取消/失败不改数据；1.5.13 直接使用组件实际注册的消息接口及最小面板消息权限，不再依赖旧确认兼容层，不增加文件访问权限。恢复确认框的打开、等待、取消已有 1.5.13 安装版证据，其他分支不由此推定通过。

## 代码入口

| 范围 | 入口 |
| --- | --- |
| 后端格式/资源/目录 | `src-tauri/src/pet_packs.rs` |
| 命令/修订/多窗口同步 | `src-tauri/src/pet_commands.rs` |
| 配置防覆盖 | `src-tauri/src/repository.rs` |
| 资源上下文与名字模板 | `src/pet/petProfile.ts` |
| 用户页面 | `src/panel/MyPetPage.tsx` |
| 动画、切换与学习反馈 | `SpriteAnimator.tsx`、`PetWindow.tsx`、`LearningDesktopStage.tsx` |
| 打包 | `scripts/package_pet.mjs` |

## 自动回归

2026-09-07 已通过的阶段性检查：

- 前端 244 项测试及离线 ZIP 确定性测试；包含 18 个缺省装扮映射、晚到结果、静态图回退、疲劳中切换后补水恢复，以及学习反馈中切换有/无学习能力包。页面测试验证订阅先于读取、校验后改名、预览许可/能力、确认不自动切换以及取消；启动订阅回归验证旧挂载清理不会取消新快照加载。
- Rust 工作区全量测试通过；主程序 317 项通过、1 项原有忽略项。关闭默认功能构建：主程序 235 项通过、1 项原有忽略项。
- 宠物后端新增测试使用真实既有图集验证四种能力组合、重复导入、取消/篡改、重启、当前包删除保护、昵称隔离、配置防覆盖、备份恢复、资源白名单、损坏检测和重新导入修复。
- 移除闲置包先暂存其目录，专用配置写入失败则恢复目录且不推进修订号；覆盖模拟持久化失败回滚。此测试不等同于真实磁盘满/硬件故障。
- 备份恢复与宠物配置写入按同一互斥顺序处理，恢复在后台执行并同步推进修订，避免恢复后的选择被先前改名快照覆盖；素材目录不可用不会阻止数据库恢复。
- 最终前端检查 244 项通过，完整 `npm run verify`、Rust 工作区全量测试和 Tauri 生产构建通过；少量最后的切换清理改动又通过前端完整检查及生产重建。无默认功能 Rust 测试单独核验。
- 候选安装包：`src-tauri/target/release/bundle/nsis/圆圆提醒_1.5.9_x64-setup.exe`，18,879,032 字节，2026-09-07 15:13（本机时区）。SHA-256：`2044FF25E70E6E14EC4697B3BAB3A23DE1CC07D27D7C46F38581AD58947E1910`。候选仍处于 development 通道，未发布，不代表下述实机矩阵已通过。

## Windows 实测与发布边界

已使用独立 runtime-qa 数据目录启动真实 Tauri 测试版，观察“设置 → 我的宠物”、现有形象实际播放、Unicode 昵称保存及设置文案同步。测试数据和成品测试包留在忽略的 `work/my-pet-qa/`，没有修改正式用户数据库。

原生选择器已显示，但未完成成品包确认与切换的桌面验收。桌面工具随后报告 `foreground window did not report a process id`，重新选择窗口后仍未恢复可靠控制，因此停止继续使用失效窗口引用，不将此轮计作完整 GUI 通过。

以下项目在完成实际验证之前保持待验收，不以单元测试或 WebView 缩放代替：

- 最终安装版的导入、确认、切换、重启、移除和恢复默认完整操作。
- 100% / 125% / 150% 真实 Windows 缩放下宠物与页面播放，以及减少动态、键盘及读屏人工听读。
- 真实满盘/磁盘故障、安装中断及崩溃窗口的现场回归。
- 最终安装版专注、提醒、学习和切换的全场景人工组合验收。

本记录区分代码实现、自动验证和实际桌面验证；未完成矩阵前不宣称完整发布验收通过。运行目录名沿用历史路径，不代表产品版本仍为 1.5.5。

## 后续实机验收操作

在独立 Windows 测试账户或虚拟机中安装最终候选，不覆盖日常数据库：

1. 将内置形象改名为“汤圆🐈”，重启；检查宠物标题、设置称呼、菜单与下一条通知。创建正文含“圆圆”的提醒，确认正文未变。
2. 分别导入完整包、缺学习包、缺装扮包；检查许可预览、取消无残留、确认不自动切换、重复导入只留一个条目。逐项预览实际存在的动作。
3. 两个形象分别改名后交替切换；重启后再次检查名字。移除闲置包，确认原始文件仍存在；恢复默认，确认其他形象和昵称保留。
4. 专注到疲劳后切换，完成补水/用餐提醒并检查恢复；水疗、睡眠、等待用户和玩具互动期间快速切换，检查旧道具及回调无残留。
5. 学习答题反馈中切换；再用缺学习包、关闭动画、减少动态分别复测，确保只记录一次答案并正常进入下一题。
6. 在**测试数据**中损坏已安装的必需图集，确认降为同宠物静态图；再损坏静态图，确认回退内置圆圆。恢复缺包备份后重新导入原包，确认原选择及昵称恢复。
7. 在 100% / 125% / 150% Windows 显示缩放下逐次检查页面、宠物、原生文件选择器与动画播放；完成键盘焦点和读屏听读。

每项记录候选安装包哈希、构建能力、Windows 缩放、观察结果及错误；不要将模拟失败或缩放截图冒充真实故障/系统缩放验收。

## 2026-09-08 情境视觉候选验收补充

后续已完成端点注册及两个循环行修复，22 项素材工具回归和全 18 行独立静态复核通过；真实播放、系统缩放和安装版组合仍未通过，正式素材不变。最新细节及告警见[情境图集衔接修复续验](SCENE_ATLAS_REPAIR_ACCEPTANCE_20260908.md)，以下原始失败保留。

桌面输入在新的 Windows 输入法隐私提示处暂停，等待用户处理，未由自动化选择隐私选项或重启绕过。暂停期间定位疲劳图集断边的装配根因，完成完整姿态提取及编码后像素校验，12 项独立工具回归通过。12 行原始素材可重新正确提取，另 6 行源图缺陷制作了完整行候选；候选虽然通过 144 格结构检查，夜间动作组仍有明显体型跳变，不能放行。

当前应用素材及版本仍为 1.5.19 development，未将工作目录候选写入正式图集。最终独立静态复核还确认工作组低伏/直立姿态的跳变；需解决夜间及工作动作组衔接、检查实际动画及缩放，并继续原安装版矩阵。此前全量测试和构建仅属于原 1.5.19，不能替尚未安装的新素材背书。详见 `MY_PET_E2E_ACCEPTANCE_V1.md` 后续记录。

## 2026-09-08 桌面恢复与启动补偿修复

用户处理输入法隐私提示后恢复实际验收。1.5.14 中文昵称“从”已保存，并同步到宠物菜单与学习导入窗口；原提醒内容未变化，但尚未覆盖正文含“圆圆”、系统通知或读屏的完整要求。

同时发现“提醒存在时启动学习被拒绝 → 完成提醒 → 再次启动仍被遗留会话阻挡”的真实故障。1.5.15 修复候选为新建会话补上精确修订的失败收尾和展示释放，不删除历史，并在前端刷新状态、显示中文原因及旧会话恢复入口。定向回归已先失败后通过；全量自动校验、有/无学习生产构建已通过。完整安装版已实测阻挡后收尾、完成提醒后重试，以及两次 ready 错题反馈中换宠物和三题结算；仍需完成其余业务、无学习安装、系统缩放和辅助功能矩阵。原始失败与完整矩阵见 `MY_PET_E2E_ACCEPTANCE_V1.md`，不把该阶段进展写成全部验收完成。

继续实测发现 1.5.15 学习完成页中关闭动画会让原生窗口缩小并裁切黑板。1.5.16 候选增加设置后的扩展尺寸同步，保留学习组件及焦点、按最新宠物尺寸恢复，并使过期布局计算失效；十一项新回归、完整自动校验、Rust 全工作区/无学习测试及两种生产构建已通过。第十二轮断网安装版已实测反馈中关闭动画、无动画时完整/基础形象双向切换与完成页反复开关动画：黑板完整、可以继续答题、三题恰好三次作答/复习、无额外照料记录。系统减少动态、真实缩放、无学习安装及其他矩阵项目仍待验收，不代表全部升级验收完成。
