# 1.5.28 逐文件差异处理表

以完整圆圆1.5.27工作区与冻结来源 `d0317cf5e9d8324aaee0461d84b4589c4a2fdcc3` 比较。涵盖 298 个有差异的文件。原始与候选文件 SHA-256 另存本地交付的 module-disposition.json。

运行时采用动态宠物昵称，不引入来源静态品牌替换；内置素材和服装全部保留。来源工具的旧独立品牌检查已适配统一应用身份，独立应用工具保留而不声明其安装流程通过。历史验收文件保持各自原版本。

| 文件 | 处理 | 理由 |
| --- | --- | --- |
| `.github/workflows/release.yml` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `AI_CUSTOMIZATION_PROMPT.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `ASSETS_LICENSE.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `CHANGELOG.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `JIAOJIAO_ASSETS_LICENSE.md` | 宠物包 | 来源许可原字节置于饺饺包 LICENSE.txt，不替换圆圆许可 |
| `PRIVACY.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `README.en.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `README.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `SECURITY.md` | 等价保留 | 保持圆圆应用身份、权限、官方素材与历史资料 |
| `THIRD_PARTY_LICENSES.txt` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `customization/PET_CUSTOMIZATION_AGENT_PROMPT.zh-CN.md` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/README.md` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/LEARNING_IMPORT_PROMPT.zh-CN.md` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/cards.template.csv` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/generate-learning-pack-report.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/interop-fixtures/canonical-edge.learning-pack.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/interop-fixtures/codex.learning-pack.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/interop-fixtures/kimi.learning-pack.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/interop-fixtures/workbuddy.learning-pack.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/learning-pack-interop.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/learning-pack-validation.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/learning-pack-validation.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/learning-pack.schema.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/learning-pack.synthetic.example.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/learning/validate-learning-pack.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/pet-request.schema.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/pet-request.synthetic.example.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/pet/functional-regression.template.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/pet/identity-lock.template.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/pet/official-asset-hashes.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/platforms.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `customization/product-brand.schema.json` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `docs/CUSTOMIZE_YOUR_PET.md` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `docs/MY_PET_E2E_ACCEPTANCE_V1.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/MY_PET_UPGRADE_V1.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/P0_RUNTIME_ENDURANCE_GATE_2026-08-09.md` | 等价保留 | 保留原历史验收/RFC，不挪用来源通过结论 |
| `docs/PET_PACK_SPEC.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/SCENE_ATLAS_REPAIR_ACCEPTANCE_20260908.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/SCENE_WARDROBE_DEVELOPMENT_PLAN_V1.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/SCENE_WARDROBE_DEVELOPMENT_PLAN_V2.md` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/images/scene-wardrobe/scene-wardrobe-core-v2.png` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/images/scene-wardrobe/scene-wardrobe-routine-v1.png` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/images/scene-wardrobe/scene-work-fatigue-v1.png` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/images/scene-wardrobe/scene-work-fatigue-v2.png` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/learning/GEN_000_CONTENT_PACK_V1.md` | 排除 | 独立应用静态品牌模块不适用于共享主程序；昵称继续使用现有动态宠物配置 |
| `docs/learning/IMPLEMENTATION_STATUS.md` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `docs/learning/QA_MATRIX.md` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `docs/release/COMMUNITY_STABLE_ACCEPTANCE_V1.json` | 排除 | 独立应用静态品牌模块不适用于共享主程序；昵称继续使用现有动态宠物配置 |
| `docs/release/COMMUNITY_STABLE_ACCEPTANCE_V1.template.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `docs/release/COMMUNITY_STABLE_RELEASE_POLICY_V1.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `docs/release/MSIX_STORE_ONBOARDING_RUNBOOK.md` | 等价保留 | 保留原历史验收/RFC，不挪用来源通过结论 |
| `docs/release/MSIX_STORE_SUBMISSION_INPUTS_V1.template.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `docs/release/THIRD_PARTY_LICENSE_POLICY_V1.json` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `docs/unified/UNIFIED_PRODUCT_V1_5_RFC.md` | 等价保留 | 保留原历史验收/RFC，不挪用来源通过结论 |
| `index.html` | 等价保留 | 保持圆圆应用身份、权限、官方素材与历史资料 |
| `package-lock.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `package.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `product-brand.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `product-version.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `public/assets/pet/fallback.png` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `public/assets/pet/learning-atlas.webp` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `public/assets/pet/life-atlas.webp` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `public/assets/pet/pet-manifest.json` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `public/assets/pet/scene-atlas.webp` | 保留基线 | 保留圆圆专有情境图集，来源缺少该图集，不替换正式素材 |
| `public/assets/pet/sleep-atlas.webp` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `public/assets/pet/spritesheet.webp` | 宠物包 | 饺饺原图/动作清单进入独立包；主程序保持圆圆原图，清单仅做引用/静态帧适配 |
| `scripts/assert_runtime_qa_exclusive.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_community_stable_e2e_stage.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_community_stable_runtime_baseline_candidate.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_community_stable_sandbox_helper_guarded.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_community_stable_v132_sandbox_helpers_guarded.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_msix_preview.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_msix_store_candidate.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_runtime_qa_learning_guarded.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/build_scene_atlas.py` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/build_windows_upgrade_baseline.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/community_release_contract.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/community_stable_acceptance_contract.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/auto-customization-core.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/auto-customization.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/auto-customization.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/complete-customization-step.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/customization-state.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/customization-state.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/package-customization.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/package-customization.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/pet-evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/pet-evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/private-input-boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/private-input-boundary.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/resume-customization.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/run-customization-verification.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/show-customization-status.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/source-snapshot.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/start-customization.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/customize/verify-private-input-boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/fixtures/my-pet-e2e-words.csv` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/generate_msix_store_license_review_packet.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_msix_store_release_manifest.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_pet_pack_qa.py` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_release_license_review_packet.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_release_manifest.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_release_preflight.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/generate_release_preflight.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/inspect_nsis_payload.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/learning_pack_spike_boundary_policy.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_community_stable_learning_runtime.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_diagnostic_card.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_learning_accessibility_matrix.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_learning_memory.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_learning_sleep_wake.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/measure_release_cold_start.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/measure_runtime_baseline.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/my_pet_sandbox_guest.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/package_pet.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/package_pet.node-test.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/prepare_community_release.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_release.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_stable_acceptance_draft.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_stable_acceptance_draft.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_stable_e2e_stage.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_stable_e2e_stage.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_community_stable_runtime_baseline_candidate.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_msix_store_submission_inputs.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/prepare_my_pet_e2e.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/prepare_my_pet_invalid_fixtures.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/prepare_my_pet_invalid_fixtures.node-test.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/probe_release_first_start_recovery.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/probe_release_install_failure_recovery.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/probe_release_uninstall_data_choice.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/probe_release_upgrade_rollback.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/product_brand_contract.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_runtime_baseline.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_sandbox_data_probe.ps1` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_sandbox_data_probe_guarded.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_sandbox_data_probe_host.ps1` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_v132_sandbox_probe_guarded.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_community_stable_v132_sandbox_probe_host.ps1` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/run_windows_upgrade_e2e_sandbox.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/run_windows_upgrade_e2e_sandbox_host.ps1` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/sync_product_brand.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/sync_product_brand.node-test.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/sync_unified_product_version.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/sync_unified_product_version.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/test_build_scene_atlas.py` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/unified_candidate_contract.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/validate_pet_pack.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `scripts/verify_ai_disabled_boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_ai_disabled_boundary.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_acceptance.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_acceptance.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_artifact_binding.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_artifact_binding.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_authority.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_e2e_stage.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_installed_e2e.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_installed_e2e.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_learning_runtime_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_learning_runtime_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_runtime_baseline_candidate.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_runtime_baseline_candidate.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_runtime_source_binding.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_v132_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_community_stable_v132_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_first_start_recovery_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_first_start_recovery_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_accessibility_matrix_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_accessibility_matrix_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_crash_recovery_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_crash_recovery_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_interaction_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_interaction_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_pack_spike_boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_pack_spike_boundary.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_reminder_preemption_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_reminder_preemption_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_sleep_wake_evidence.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_learning_sleep_wake_evidence.node-test.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_msix_store_submission_inputs.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_msix_store_submission_inputs.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_product_brand_copy_boundary.mjs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_release_boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_task_expression_motion_evidence.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_task_expression_motion_evidence.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_unified_candidate.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_unified_product_boundary.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_unified_product_boundary.node-test.mjs` | 工具保留 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `scripts/verify_windows_upgrade_e2e.mjs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/Cargo.lock` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src-tauri/Cargo.toml` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src-tauri/capabilities/default.json` | 等价保留 | 保持圆圆应用身份、权限、官方素材与历史资料 |
| `src-tauri/capabilities/panel-confirmation.json` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/crates/learning-pack-spike/src/lib.rs` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src-tauri/icons/128x128.png` | 排除 | 饺饺应用图标不用于宠物包；保留圆圆安装器和托盘图标 |
| `src-tauri/icons/128x128@2x.png` | 排除 | 饺饺应用图标不用于宠物包；保留圆圆安装器和托盘图标 |
| `src-tauri/icons/32x32.png` | 排除 | 饺饺应用图标不用于宠物包；保留圆圆安装器和托盘图标 |
| `src-tauri/icons/icon.ico` | 排除 | 饺饺应用图标不用于宠物包；保留圆圆安装器和托盘图标 |
| `src-tauri/migrations/013_meal_reminder_category.sql` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/msix/AppxManifest.preview.xml` | 等价保留 | 保持圆圆应用身份、权限、官方素材与历史资料 |
| `src-tauri/msix/AppxManifest.store.xml` | 等价保留 | 保持圆圆应用身份、权限、官方素材与历史资料 |
| `src-tauri/src/backups.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/bin/installed_candidate_qa.rs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `src-tauri/src/brand.rs` | 排除 | 独立应用静态品牌模块不适用于共享主程序；昵称继续使用现有动态宠物配置 |
| `src-tauri/src/commands.rs` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/companion_core.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/companion_runtime.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/connector_discovery.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/connector_project_inspection.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/connector_tool_trust.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/installed_candidate_qa.rs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `src-tauri/src/learning/migrations/008_generic_learning_packs.sql` | 迁入 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/mod.rs` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/models.rs` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/pack.rs` | 迁入 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/quiz.rs` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/repository.rs` | 迁入 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning/repository/portability.rs` | 迁入 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src-tauri/src/learning_session_start_tests.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/lib.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/local_data_cleanup.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/logging.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/migration_qa.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/models.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/notifications.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/pet_commands.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/pet_packs.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/pet_packs_tests.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/presentation_arbiter.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/presentation_runtime.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/repository.rs` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src-tauri/src/runtime_qa.rs` | 工具适配 | 迁入来源、恢复、校验、数据探针及打包能力；默认宠物包路线，独立应用工具仅在单独配置副本使用 |
| `src-tauri/src/scheduler.rs` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src-tauri/src/state.rs` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src-tauri/src/tray.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/src/windows.rs` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src-tauri/tauri.conf.json` | 适配 | 同步统一候选1.5.28配置、依赖、发布材料或文档；身份/数据库目录不变，历史验收不冒充本轮 |
| `src/brand.test.ts` | 排除 | 独立应用静态品牌模块不适用于共享主程序；昵称继续使用现有动态宠物配置 |
| `src/brand.ts` | 排除 | 独立应用静态品牌模块不适用于共享主程序；昵称继续使用现有动态宠物配置 |
| `src/learning/LearningDesktopStage.test.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/learning/LearningDesktopStage.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/learning/LearningView.test.tsx` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src/learning/LearningView.tsx` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src/learning/learning.css` | 迁入 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `src/lib/backend.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/lib/backend.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/lib/confirmation.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/lib/confirmation.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/main.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/panel/AiCompanionStatus.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/AiCompanionStatus.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/ConnectorDisconnectReview.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/ConnectorDisconnectReview.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/ConnectorDiscoveryStatus.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/ConnectorDiscoveryStatus.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/MyPetPage.test.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/panel/MyPetPage.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/panel/SafeDisplayDocument.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/TaskPanel.e2e.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/TaskPanel.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/panel/connectorDisconnectUi.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/panel/panel.css` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/CompanionPropStage.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/ExpressionLab.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/PetWindow.interaction.e2e.test.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/PetWindow.layout.e2e.test.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/PetWindow.sceneWardrobe.e2e.test.tsx` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/PetWindow.sleep.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/PetWindow.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/SpriteAnimator.runtime.test.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/SpriteAnimator.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/SpriteAnimator.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/alertStage.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/alertStage.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/companionMotion.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/companionPresentation.test.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/companionPresentation.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/expressionDirector.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/interactionMotion.test.ts` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/interactionMotion.ts` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/manifest.test.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/manifest.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/pet.css` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/petIntent.test.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/petIntent.ts` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/pet/petProfile.sync.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/petProfile.test.tsx` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/petProfile.ts` | 适配 | 合并逗猫棒时间/距离推进与无障碍反馈，保留状态恢复；补齐基础动作专注静止与首切预加载 |
| `src/pet/sceneWardrobe.test.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/pet/sceneWardrobe.ts` | 保留基线 | 保留圆圆完整工作区已有修复与新增工具 |
| `src/support/SupportLab.test.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/support/SupportLab.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/support/SupportSortBoundaryLab.tsx` | 等价保留 | 保留圆圆已有状态清理、动态昵称、固定数据目录或情境动作实现；来源独立品牌改写不迁入 |
| `src/types.ts` | 适配 | 迁入知识包协议、学习库008、预览/更新/导出；保留圆圆昵称与互动租约，并修复声明回忆题和监听器清理 |
| `docs/unified/UNIFIED_1_5_28_CANDIDATE.md` | 新增适配 | 本轮增加的宠物包交付、统一身份门、静止/预加载回归或候选边界材料 |
| `scripts/customize/pet-package.mjs` | 新增适配 | 本轮增加的宠物包交付、统一身份门、静止/预加载回归或候选边界材料 |
| `scripts/customize/pet-package.node-test.mjs` | 新增适配 | 本轮增加的宠物包交付、统一身份门、静止/预加载回归或候选边界材料 |
| `scripts/verify_unified_pet_identity.mjs` | 新增适配 | 本轮增加的宠物包交付、统一身份门、静止/预加载回归或候选边界材料 |
| `src-tauri/src/learning/unified_tests.rs` | 新增适配 | 本轮增加的宠物包交付、统一身份门、静止/预加载回归或候选边界材料 |
