# 圆圆 1.5.28 本地候选

以圆圆完整 1.5.27 工作区为基线，迁入冻结来源 `d0317cf5e9d8324aaee0461d84b4589c4a2fdcc3` 的通用学习能力与交互改进。饺饺作为独立数据宠物包交付；圆圆的图集、图标、服装、热身与静态形象均沿用基线。

## 行为与兼容边界

- 应用标识仍为 `com.yuanyuan.reminder`，两只宠物共用主库、学习库、设置和进度。
- 学习库迁移 7 → 8，与主库独立；迁移失败回滚且不开放学习写入。
- CSV、原生 JSON 与 learning-pack v1 均可预览后导入。内容更新保留卡片身份；答案或排程版本改变的重置数量先展示，同卡重复导入不重置；排程版本倒退被拒绝。移除的卡片按包更新停用，不抹去历史。
- 提交前重新核对文件；预览后变化必须重新预览。导入可取消，批次在事务内写入。
- 宠物包清单只承载数据，缺少情境图集时使用自身基础动作；工作专注持续静止。首次切换等待声明的图集加载完毕。
- 定制默认输出 `.yuanyuan-pet`，保留来源、哈希锁、恢复与校验。独立应用工具仅为单独配置的工作副本保留，不纳入此候选的独立应用交付声明。

## 本轮证据与尚未通过的门

本地交付目录含实际安装器、饺饺包、SHA-256、逐文件差异处理表、基线清单、自动测试日志、升级副本报告和 PR 草稿。私有数据库与原始数据备份仅存放本机，不进入 Git 或 PR。

自动测试包括知识包非法输入、重复/更新/停用、重置提示、预览后文件变化、取消、事务回滚、导出恢复、迁移失败及互动切换；另有 30 秒静止渲染检查。实际旧学习库副本中的 4,533 张卡片及排程经过迁移逐值比较。它们不替代原生安装与播放验收。

本轮 Computer Use 被用户通过物理 Esc 停止，随后停止原生界面操作。因此以下项目保持待验收：

| 必需项目 | 当前状态 |
| --- | --- |
| 同一饺饺包在隔离 1.5.27 与 1.5.28 中预览、导入、切换、重启与损坏恢复 | 待原生验证 |
| 两只宠物的全部动作、提醒/学习/睡眠打断恢复、专注至少 30 秒及关闭动画 | 待原生验证 |
| Windows 原生系统缩放 100%、125%、150% 的布局、点击范围、播放及首次切换 | 待原生验证 |
| 实际候选安装器升级与“1.5.27 程序＋升级前完整数据”回退 | 待隔离安装验证 |

候选不可正式采用。正式安装、推送、合并及公开发布均未执行；完成上述门并获得相应授权后再进行。

## 配套回退

回退须先正常退出程序，将升级后的完整应用数据另存，再恢复 1.5.27 程序与升级前的完整数据目录（含主库、学习库、WAL/SHM、宠物包和选择配置）。不要把第 8 版学习库直接交给旧程序。数据库的独立恢复只用于排查，不能替代完整目录回退。必须先在隔离环境验证程序版本、数据数量、宠物选择、重启和数据库完整性，再考虑正式环境操作。
