# v1.4.0 签名与发布渠道决策包

状态：已调整为低成本分阶段方案；当前 NSIS 仅作未签名测试版，正式目标为 Microsoft Store MSIX

日期：2026-08-10

历史冻结候选摘要：`6F41A4B19BD0E8DFC21432CD23C893D17AFB5D2C5C9F42C632567E2BFD2D2225`（对应文件当前不在工作区且尚未发布，不能由同名重建文件冒充）

## 1. 必须先做这项决定的原因

项目采用 `low_cost_staged` 策略：v1.4.0 NSIS 将在源码和候选冻结后通过 GitHub Releases 作为明确标注的未签名测试版分发，并同时提供 SHA-256；当前公开 Releases 尚无 v1.4.0，旧冻结摘要对应的本地文件也已不存在，因此不得声称测试版已经发布。正式稳定版计划改为 Microsoft Store MSIX，由 Store 托管签名。独立 MSIX 技术预览已经完成结构验证，但 Store 分配身份、运行验收与正式提交候选尚未建立，因此当前 NSIS 的正式 `selectedChannel` 保持 `pending`，严格发布门继续关闭。

当前 v1.4.0 冻结范围是直接分发的 Windows x64 NSIS 安装包与独立主程序。改变为 MSIX 属于新的安装、升级、数据迁移和卸载边界，不能把现有 NSIS 证据直接沿用过去。

## 2. 当前可选渠道

| 渠道 | 当前候选可沿用 | 费用与地区 | SmartScreen | 主要影响 |
| --- | --- | --- | --- | --- |
| Microsoft Store MSIX | 否，需要新候选 | Store 开发者新 onboarding 当前可免费注册；微软托管并重签 | Store 安装不出现下载型 SmartScreen 警告 | 需要新增 MSIX、身份、更新、数据迁移和与 NSIS 并存/切换设计 |
| Microsoft Store EXE/MSI | 部分 | 仍需自行购买受信 CA 证书并托管不可变版本 URL | Store 流程可信，但 EXE/MSI 仍须发布者签名 | 不能绕过当前 Authenticode 门，也没有 Store 托管更新 |
| Artifact Signing Public Trust | 是 | Basic 当前约 9.99 美元/月、每月 5,000 次；组织仅限美国、加拿大、欧盟和英国，个人仅限美国和加拿大 | 初期仍可能出现未知应用，信誉逐步积累 | 无硬件令牌，适合直接下载和自动化签名，但有身份地区限制 |
| 传统 CA OV 代码签名证书 | 是 | 微软当前比较页给出的典型成本约 150–300 美元/年，地区覆盖更广 | 与 Artifact Signing 一样需要积累信誉 | 最适合不满足 Artifact Signing 地区条件且必须保留 NSIS/便携版的发布者 |
| 传统 CA EV 代码签名证书 | 是 | 通常明显高于 OV | 自 2024 年起不再提供首次下载即时 SmartScreen 绕过 | 仅因企业采购或其他身份要求选择，不应为了 SmartScreen 溢价 |
| SignPath Foundation OSS | 理论上可签 EXE，但当前不应依赖 | 符合条件的开源项目免费，证书发布者为 SignPath Foundation | 使用受信代码签名证书，但仍受信誉与平台判断 | 当前安装包包含非 OSI、仅限个人非商业使用的圆圆素材，与其“所有组件采用 OSI 许可、无专有组件”条件冲突；申请也没有通过保证 |

项目负责人于 2026-08-10 将方案调整为“未签名 NSIS 测试版 + SHA-256，后续 Microsoft Store MSIX 正式版”。当前不采购 OV/EV 证书；传统 CA 方案仅保留为未来需要独立正式下载时的备选。

## 3. 条件化建议

### A. 保持 v1.4.0 NSIS 与便携版，发布者位于中国或其他 Artifact Signing 不支持地区

建议选择 `ca_code_signing_certificate`，购买 OV 证书并以经验证的个人或组织法定名称签名。不要仅为 SmartScreen 购买 EV。此路径改动最小，可以复用当前安装、升级和卸载证据，但签名后三项正式产物哈希变化，所有候选绑定人工包必须重新生成和执行。

### B. 发布者满足 Artifact Signing Public Trust 地区与身份条件

建议选择 `artifact_signing_public_trust` Basic。先完成 Azure 计费资料与法定名称一致性、身份验证和证书配置，再把签名接入受控构建。不要承诺首次下载无 SmartScreen 提示。

### C. 预算必须为零，并接受延后当前 NSIS 正式发布

建议把 Microsoft Store MSIX 作为独立后续候选，而不是修改当前 v1.4.0 证据。先做 MSIX 技术刺探，验证托盘、开机启动、通知、单实例、WebView2、数据目录、备份恢复、升级和卸载选择，再决定是否以 Store 为主渠道。

### D. 申请 SignPath Foundation

只有在把圆圆图像、图标和截图更换为 OSI 兼容素材或取得满足其政策的重新许可，并补齐公开代码签名政策、MFA、作者/复核者/批准者角色及可验证构建后，才值得作为并行申请。它不应阻塞当前发布路径。

## 4. 负责人需要冻结的五个输入

1. `ownerType`：`individual` 或 `organization`；
2. `legalRegion`：法定个人或组织所在国家/地区；
3. `legalPublisherName`：准备接受身份验证并显示给用户的精确法定名称；
4. `distributionGoal`：`direct_download`、`store_msix` 或 `both`；
5. `annualSigningBudget`：可接受的年度签名与发布预算。

这些输入必须由人类负责人提供。不得从 Git 提交名、邮箱、时区、仓库名或 AI 推断法定身份与地区。

## 5. 选择后的工程动作

1. 当前 NSIS 只以测试版发布，文件名、字节数、SHA-256 和“未签名”状态必须同时披露；
2. 测试版不得标记为稳定版，不得声称已通过 SmartScreen 或正式发布门；
3. 独立 MSIX 技术预览已完成打包、解包、清单、哈希和精确载荷边界验证；下一步在受支持的干净 Windows 11 环境验证安装、应用身份、托盘、开机启动、通知、单实例、WebView2、数据目录、备份恢复、升级和卸载；
4. 完成 Microsoft Store 开发者账户与应用身份配置，由 Store 托管正式包签名；
5. 为 MSIX 重新生成清单、许可证、升级回退、安全软件和无障碍证据，不沿用当前 NSIS 的候选哈希；
6. MSIX 专用正式发布门通过后，才把 `selectedChannel` 从 `pending` 冻结为 `microsoft_store`。

当前已补齐正式 Store 链的关闭失败入口：Partner Center 六项公开身份的双重人工来源声明、不可覆盖写入与正式校验、匿名公开链接 HTTP 200/七天时效/固定来源/远端隐私原文一致性证据、干净提交源码构建、候选哈希/载荷独立复核、独立隐私政策与 listing/四图/市场/能力/IARC 输入合同、逐文件 Store 发布清单与 SBOM/许可绑定、不可复用 NSIS 签字的 Store 专用许可证复核包/人工验收、NSIS→MSIX/备份恢复/更高版本更新/卸载保留/应用内显式删除五场景数据生命周期验收、Defender 原包/解包双扫描、两家不同厂商第三方安全产品人工矩阵、一次性环境临时受信副本的安装/启动/卸载/候选载荷血缘与 WACK、十三项人工预提交矩阵、认证后 Microsoft 重签包最终回归，以及认证/上架/IARC/runFullTrust/公开产品链接脱敏证据与只允许改变 `selectedChannel` 的最终渠道切换门。应用内完整数据删除入口现已采用逐字确认、不可恢复勾选、最终系统确认和退出后固定数据根清理实现；公开仓库主页和 Issues 已匿名返回 HTTP 200，但远端 `main` 尚无 `PRIVACY.md`，隐私链接为 404；Partner Center 实际身份、正式候选及具名人工证据仍不存在，故正式 Store 入口继续保持待办；具体操作见 `MSIX_STORE_ONBOARDING_RUNBOOK.md`。

## 6. 官方资料

- [Microsoft：代码签名选项](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)
- [Microsoft：SmartScreen 发布者与文件信誉](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)
- [Microsoft：Artifact Signing Public Trust 快速入门与地区限制](https://learn.microsoft.com/en-us/azure/artifact-signing/quickstart)
- [Microsoft：Artifact Signing SKU 价格与配额](https://learn.microsoft.com/en-us/azure/artifact-signing/how-to-change-sku)
- [Microsoft：Microsoft Store Win32/MSIX 分发选择](https://learn.microsoft.com/en-us/windows/apps/distribute-through-store/how-to-distribute-your-win32-app-through-microsoft-store)
- [Microsoft：Store 开发者账户](https://learn.microsoft.com/en-us/windows/apps/publish/partner-center/open-a-developer-account)
- [SignPath Foundation：开源项目条件](https://signpath.org/terms.html)
