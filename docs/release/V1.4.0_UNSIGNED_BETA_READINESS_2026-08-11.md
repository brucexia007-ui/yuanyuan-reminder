# v1.4.0 未签名测试版就绪审计

> 历史冻结：本审计只保留 v1.4 的 `NO-GO` 证据，不得再据此冻结或发布安装包。当前流程见 `docs/unified/UNIFIED_CANDIDATE_RUNBOOK.md`。

日期：2026-08-11
结论：`NO-GO`，低成本渠道策略不变，但 v1.4.0 测试版尚未形成可发布的不可变候选。

## 已确认事实

- 公开仓库 `brucexia007-ui/yuanyuan-reminder` 的 Releases 当前只有 v1.3.0 和 v1.3.2，没有 v1.4.0；
- `V1.4.0_UNSIGNED_BETA_SHA256SUMS.txt` 冻结的候选摘要为 `6F41A4B19BD0E8DFC21432CD23C893D17AFB5D2C5C9F42C632567E2BFD2D2225`，记录字节数为 12,503,802；
- 工作区内已找不到与该摘要对应的安装包副本；
- 2026-08-11 本次审计首次取样时，为完成工程回归而执行的普通 Tauri 构建已覆盖默认 NSIS 输出；当时观察到的文件为 12,514,776 bytes，SHA-256 为 `DF970A2A66CF631C49AFEDC2E882F69551FCD3018E5D59567688D66E8EE462CA`。默认输出仍可能被后续普通构建继续覆盖，因此该数值只用于证明旧候选已丢失，不表示当前或冻结候选；
- 该取样来自包含大量未提交改动的 `feat/learning-preview-foundation` 工作区，不是冻结发布源码，不能替换、冒充或上传为既有候选。

## 发布前必须完成

1. 按 `V1.4.0_RELEASE_SOURCE_SCOPE_V1.json` 和 `V1.4.0_SOURCE_SPLIT_AUDIT_2026-08-11.md` 把稳定发布维护与 Learning Preview 拆开，形成 `main` 上干净、可追溯且仍使用主库 schema 11 的提交；
2. 先运行 `release:source-scope:verify`，再运行 `release:unsigned-beta:freeze`；冻结命令也会自动重跑源码范围门、完整验证、Rust 测试、正式构建和安装载荷复核，并在构建前后再次确认提交、范围报告及工作区未变化；
3. 使用不可覆盖的 `src-tauri/target/unsigned-beta/1.4.0/` 完成候选绑定的许可证、安全扫描和安装/卸载验收，不得继续引用默认构建目录；
4. 只使用冻结目录自动生成的字节数、`SHA256SUMS.txt` 和 `RELEASE_NOTES.md`，禁止手工沿用旧摘要或弱化未签名披露；
5. 以 GitHub prerelease 发布安装包与 `SHA256SUMS.txt`，明确标注“未签名测试版 / 未知发布者 / 不属于稳定渠道”；
6. 发布后运行 `release:unsigned-beta:github:capture`，匿名复核标签提交、预发布标记、正文、精确资产集合、下载字节和 GitHub API digest，再把冻结报告及公开发布报告共同用于 NSIS→MSIX 数据迁移验收。

在上述步骤完成前，稳定渠道继续保持 `pending`，Microsoft Store MSIX 仍是计划中的正式稳定渠道。
