# 圆圆智能陪伴P0基线记录（2026-08-03）

记录状态：初始基线已建立，发布级冻结尚未完成<br>
Git提交：`11841b88cf7b3e6d10502fd0158401e2c02167ae`<br>
应用版本：v1.4.0构建候选版<br>

## 工作区边界

- 开始P0前存在约27项有效未提交成果；新增最终设计、开发分解和P0代码/ADR后，Git短状态会按目录聚合显示更多条目；
- 未执行 `git reset`、`git clean`、checkout恢复或其他回退操作；
- v1.4.0既有代码修改集中在提醒管理、备份、恢复、面板和版本升级；
- 本轮新增协议crate尚未接入主程序运行时，不改变提醒和宠物行为。

## 当前工具链

| 工具 | 版本 |
| --- | --- |
| rustc | 1.97.1 (8bab26f4f 2026-07-14) |
| cargo | 1.97.1 (c980f4866 2026-06-30) |
| Node.js | v25.8.1 |
| npm | 11.11.0 |

## 加入协议crate前的基线验证

### 前端、类型与宠物资源

命令：`npm.cmd run verify`

结果：

- TypeScript检查通过；
- 9个Vitest测试文件通过；
- 45项前端测试通过；
- 35个动画和3张WebP图集验证通过。

### Rust核心

命令：`cargo test`

结果：

- 39项Rust测试通过；
- 0失败、0忽略；
- 出现1条Windows链接器信息提示，不是编译或测试失败。

### Windows安装构建

命令：`npm.cmd run tauri build`

结果：

- TypeScript和Vite生产构建通过；
- Rust release构建通过；
- NSIS安装包生成成功。

| 产物 | 大小 | SHA-256 |
| --- | ---: | --- |
| `src-tauri/target/release/yuanyuan-reminder.exe` | 20,682,240 bytes | `C922C0D29264052B07921E5F6682DA68D439F151E47A3966BEE6B76E53D60ADE` |
| `src-tauri/target/release/bundle/nsis/圆圆提醒_1.4.0_x64-setup.exe` | 12,102,688 bytes | `726906206C5179C036615603B0A000811B26A4839CB2C2A9C30386537C1B1138` |

## 第一项P0代码证据

新增 `yuanyuan-protocol` crate 后执行：

```powershell
cargo test -p yuanyuan-protocol
```

结果：17项协议测试通过，0失败，覆盖单事件输入约束以及重复、乱序、序号碰撞、新运行、终态修正、非法回退和任务身份变化。该结果不证明Bridge、HMAC、IPC或持久化已经完成。

新增 `yuanyuan-bridge` 快速路径内核后执行：

```powershell
cargo test -p yuanyuan-bridge
```

结果：6项Bridge内核测试通过，0失败，覆盖输入上限、JSON/协议拒绝、单次入队、Sink不可用/队列满/超时/拒绝和所有路径退出码0。尚未接入真实命名管道、HMAC或CLI。

## 尚未完成的基线项目

- P0-004 的严格验收工具与自动回归已完成：只读原样本、隔离副本迁移、旧列逐值摘要、生产备份恢复和强制迁移失败后的安全快照回滚均已覆盖，并修复了失败迁移事务未关闭导致半迁移结构泄漏的缺陷；当前仍未提供可确认来源的 v1.3.2 真实数据库副本，不能把合成 schema v6 回归当作真实数据证据，见 `P0_V132_DATABASE_MIGRATION_QA_2026-08-06.md`；
- P0-005“AI全部关闭”自动回归门已完成：默认安装声明不含Bridge/AI，生产前端与稳定核心只允许本地IPC；真实Repository测试证明AI不存在时提醒、专注、完成和今日列表继续工作，前端无组件时隐藏实验卡。该门已接入`verify`、发布预检和严格门，见`P0_AI_DISABLED_REGRESSION_2026-08-06.md`；
- 已用隔离`runtime-qa`配置完成AI关闭60秒工程烟测：暖系统启动到宠物窗口205.5 ms，进程树CPU平均0.0684%/P95 0.2117%，平均工作集约390.9 MiB、平均私有内存约192.7 MiB，数据库相关文件增长4,120 bytes，正式目录写入0。可交互提醒延迟基线门已完成20个真实窗口样本：后端领取P95 12,938.9 ms、领取到窗口P95 281.0 ms、总呈现P95 13,167.8 ms，均低于冻结的16,000/1,000/17,000 ms上限；报告绑定验收主程序、夹具和脚本哈希，少于20样本的负向门按设计失败。独立校验器重新计算逐样本延迟和P50/P95，并拒绝来源过期、相位/字段漂移和乐观篡改；当前报告复核退出0。默认正式二进制新配置冷启动采样器已完成并接入发布预检，但当前账户因已有正式数据而在启动前安全拒绝，仍需干净测试账户取得数值；长期趋势也未完成，见`P0_RUNTIME_BASELINE_QA_2026-08-06.md`、`P0_REMINDER_LATENCY_QA_2026-08-06.md`与`P0_RELEASE_COLD_START_QA_2026-08-06.md`；
- 完成24小时v1.4.0常驻和睡眠/唤醒恢复记录；
- 在干净Windows账户上重新安装、升级和卸载；
- 手工检查100%、150%、200%DPI及多显示器宠物/面板布局；
- 执行Defender、SmartScreen和第三方安全软件预演；
- 形成签名身份、时间戳和误报处理闭环。

## 当前结论

现有自动化和安装构建证明v1.4.0底座足以开始不接入运行时的P0协议/进程Spike，但不足以宣称v1.4.0已经完成正式发布冻结。上述未完成项继续作为P0-E01/P0-E08门禁，不能被后续智能功能测试替代。
