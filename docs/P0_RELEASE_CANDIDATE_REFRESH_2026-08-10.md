# v1.4.0 发布候选证据刷新

状态：当前候选自动门已刷新，正式发布仍为 NO-GO

日期：2026-08-10

对应范围：`V1.4.0_SCOPE_FREEZE_2026-08-10.md`

## 1. 当前候选

| 产物 | SHA-256 |
| --- | --- |
| Tauri 主程序 | `8ACD55F6C4477EB3FD0E5066F9A08FF966D1C6E8D64D419FEF7F53D7C248746D` |
| NSIS 实际安装主程序 | `BC00B42649D2B1863115ED7E3A4F3B3EC681351C49BD081CC4F6B8D13AFD4C1E` |
| NSIS 安装包 | `6F41A4B19BD0E8DFC21432CD23C893D17AFB5D2C5C9F42C632567E2BFD2D2225` |

Bridge 与 AI 原型继续标记为 `prototype_excluded`，正式安装声明不包含外部二进制或 QA 夹具。

## 2. 刷新结果

当前 `release-preflight.json` 对上述安装包报告：

- 13 项通过；
- 14 项待完成；
- 0 项失败；
- `readyForRelease=false`；
- 严格预检退出码为 2，符合预期。

本轮重新生成或重做了：

- NSIS 实际安装载荷提取与包类型/许可证边界；
- 官方 v1.3.2 与当前候选的隔离安装、升级、卸载和安全回退；
- 损坏候选、主程序占用及部分写入终止后的安装失败恢复；
- 干净测试账户默认安装路径文件转换；
- 首次启动写库中断、同候选恢复、schema v11 和规范化数据库复核；
- 真实卸载界面的默认保留数据与明确勾选删除数据两条路径；
- 干净配置三次真实窗口冷启动，P50 为 145 ms，P95 为 205.7 ms；
- AI 全部关闭边界、CycloneDX SBOM、507 个组件许可证清单和人工门候选包。

## 3. 执行边界

真实管理员账户存在圆圆产品注册，NSIS 检查按设计在任何安装前停止；本轮没有删除、覆盖或修改该账户的现有安装和正式用户数据。

候选探测随后在独立、干净、与实际令牌配置目录匹配的受控测试账户执行。Rust 元数据阶段只复用已安装工具链，产品安装、HKCU 注册、快捷方式、合成数据和清理均留在测试账户边界。每个探测完成后应用进程、安装目录、测试数据、快捷方式和自有注册项均按脚本契约清理。

## 4. 仍未关闭的本机门

### 控制面板注册

默认路径下的 1.3.2→1.4.0→卸载→回装 1.3.2 文件转换与清理通过，但测试账户在四个阶段均未观察到预期 HKCU 控制面板产品注册。升级回退报告已提升为 schema v2，并在执行安装前用随机一次性键显式探测 64 位 HKCU；当前受控令牌返回 `currentUserRegistry64Writable=false`，测试键未留下残留。因此：

- `default_install_path_transition_probe` 通过；
- `default_install_control_panel_registration` 继续待完成；
- 不把“文件安装成功”外推为“添加/删除程序注册成功”，也不把受控令牌拒绝写入误判为安装器缺陷。

后续需要在不含既有圆圆安装、HKCU 可写且不受沙箱注册隔离影响的真实交互账户复测，并在测试期间记录实际卸载键位置与显示版本。

### Defender

本轮先在普通令牌下尝试，因读取 Defender 状态被拒绝而停止；提升权限后确认 Microsoft Defender Antivirus 未启用。没有扫描报告，因此 `defender_scan` 继续待完成。需要在 Defender 已启用且定义更新完成的干净机对三项正式产物重跑候选绑定扫描。

## 5. 当前 14 项待完成门

- 默认安装路径控制面板注册；
- 发布渠道；
- 精确发布者 Subject；
- 三项正式产物有效 Authenticode；
- 同证书、发布者匹配和可信时间戳；
- RFC 3161 具名人工协议证据；
- 最终签名候选无障碍人工矩阵；
- Defender；
- SmartScreen 干净机；
- 两款非 Defender 安全产品；
- 真实旧库、物理中断、Windows 重启和安全回退完整演练；
- 许可证与素材权利具名人工复核。

签名渠道与发布者身份是多数后续人工包的共同前置条件，下一步按 [`release/V1.4.0_SIGNING_CHANNEL_DECISION_2026-08-10.md`](release/V1.4.0_SIGNING_CHANNEL_DECISION_2026-08-10.md) 冻结选择。
