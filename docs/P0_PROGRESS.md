# 圆圆智能陪伴P0实施进度

更新时间：2026-08-10<br>
当前目标：完成P0冻结与技术验证，再进入v2.0A功能开发

v1.4.0 的稳定版交付范围、明确排除项、冻结时验证结果和剩余发布阻断项已于 2026-08-10 固化在 [`V1.4.0_SCOPE_FREEZE_2026-08-10.md`](V1.4.0_SCOPE_FREEZE_2026-08-10.md)。当前源码已通过完整 `verify` 与 Tauri 正式构建，但签名、外部信任、真实升级/中断、24小时运行和具名人工验收仍未关闭，因此正式发布状态继续为 NO-GO。

当前安装包候选 `6F41A4B1…2225` 已重新完成候选绑定预检，结果恢复为 13 项通过、14 项待完成、0 项失败，严格门按设计退出 2。默认路径文件转换、首次启动恢复、卸载数据选择和三次干净配置冷启动均已刷新；升级回退报告 schema v2 现独立记录 64 位 HKCU 可写性，实测当前受控令牌为不可写，因而控制面板注册继续待办而不被误判为安装器缺陷。本机 Defender 同样未启用。完整记录见 [`P0_RELEASE_CANDIDATE_REFRESH_2026-08-10.md`](P0_RELEASE_CANDIDATE_REFRESH_2026-08-10.md)，签名与渠道选择见 [`release/V1.4.0_SIGNING_CHANNEL_DECISION_2026-08-10.md`](release/V1.4.0_SIGNING_CHANNEL_DECISION_2026-08-10.md)。

## 总体状态

| Epic | 状态 | 已有证据 | 下一项硬任务 |
| --- | --- | --- | --- |
| P0-E01 v1.4.0基线 | 进行中 | 全量前后端/资源/NSIS回归、独立账户3样本默认正式程序冷启动、隔离AI关闭运行采样器、60秒资源烟测、20样本真实窗口提醒延迟冻结门、24小时/睡眠/锁屏严格执行与独立复核器、AI全部关闭组合门及候选绑定证据 | 真实数据库副本、24小时正向运行、睡眠/锁屏真实事件、DPI和干净机安装 |
| P0-E02 架构与安全边界 | 进行中 | ADR-0001—0009、认证/控制协议、P0威胁模型、角色责任图/决策流程、开发分解和最终设计 | WS0/WS1/WS2/WS8具名负责人和接手者 |
| P0-E03 协议/进程/IPC Spike | 进行中 | 安全IPC、匿名拒绝、真实凭据/CLI、诊断ACL、存储故障恢复、启动后异步保留清理、停机、真实v1/v2子进程、熔断、核心隔离及150% DPI诊断卡真实窗口/键盘验收通过 | 历史签名二进制、真实第二账户、睡眠和长期运行、其余DPI/Narrator |
| P0-E04 连接器夹具 | 进行中 | 独立连接器crate、14份合成契约夹具、官方Hook格式/Schema复核、本机Codex词汇证据、只读安装发现、认证暂停、显式用户级安全读取、多事件/多来源所有权差异预览、无损接入/撤销、私有事务、官方目标、项目级原生选择器与正式只读确认入口、来源工具签名/发布者/分发位置与离线第二因子复核、Claude 2.1.211真实签名清单及x64实体双因子贯通、断开/撤权后维护状态机与开发交互实验台 | 双方签名/包族/密钥轮换流程、Claude历史/后续版本与arm64实体矩阵、目标版本与Codex/Claude脱敏真实事件、真实窗口与无障碍验收 |
| P0-E05 非语言原型 | 进行中 | Rust稳定核心N0—N4/固定优先级/抢占恢复/专注延后/模型工具门、本地提醒与AI任务库只读接线、30秒终态聚合/10分钟摘要冷却/10分钟长运行/24小时失效、三档日小时预算、一键安静、专注结束伸懒腰与离开后轻蹭两种无气泡仪式、首版六种固定道具、生产PetIntent动作/系统卡/计时卡/工具卡拆分、可点击脱敏任务守望台与发布阻断、三类任务提示完整/减少动态真实窗口证据 | 真实Hook/sidecar发布接线、忽略后自适应降频、多DPI/Narrator和新用户辨识率冻结 |
| P0-E06 AI/文档/记忆Spike | 进行中 | `response_intent`/`display_document` v1、恶意渲染拒绝、回环Provider、中文FTS5、AI库v2精选记忆生命周期（当前库v4）、墓碑恢复与ADR-0009 | 真实云Provider、正式记忆中心、加密备份实现与扩大中文金标准 |
| P0-E07 安抚专业复核 | 进行中 | 三/六路径硬裁剪、只听不记内存状态机、类型化支持偏好、资源包v1与无号码降级、三路径正式入口/稳定核心/自动与手动结束/事务抢占、代码级隐私证据及首轮真实Tauri窗口实拍 | 正式三路径键盘/读屏/多档DPI/辨识验收；完整模块顾问签字、真实地区资源、崩溃/诊断/备份敏感扫描与用户研究 |
| P0-E08 签名与误报 | 进行中 | 版本化发布策略、Authenticode采集、NSIS实际安装主程序提取/候选绑定、CycloneDX/许可证清单、301组件确定性许可证全文归档及安装/卸载证据、候选绑定人工许可复核包/签字契约、SmartScreen/第三方安全软件外部测试包/签字契约、签名/RFC3161协议包/无凭据签字契约、完整升级/真实中断/回退包/签字契约、签名候选无障碍人工矩阵包/签字契约、三发布产物Defender入口、预检报告、严格阻断及发布SOP | 发布主体与渠道预算、真实签名/RFC3161实际签字、可用Defender环境、SmartScreen与两款第三方安全软件的实际具名人工签字、真实旧库/物理中断/系统重启、许可证与无障碍实际人工签字和回退演练 |

## 已完成的代码增量

- Cargo根清单转换为包含当前Tauri包、`yuanyuan-protocol`和`yuanyuan-bridge`的工作区；
- 新增P0威胁模型，登记10类资产、三进程/双数据库/凭据/网络信任边界和22项威胁；每项包含入口、现有控制、剩余风险、验证方式与风险接受角色，并冻结高风险发布阻断条件；
- `yuanyuan-protocol` 定义任务事件v1、输入边界和跨事件转换决策；
- `yuanyuan-bridge` 定义快速路径、Sink抽象、脱敏结果码和fail-open退出契约；
- Bridge新增仅接受本机逻辑名称的Windows命名管道客户端和短生命周期进程硬超时看门狗；
- Bridge新增stdin静默CLI入口，无效输入、无效参数和主程序缺失均退出0且不写stdout/stderr；
- 认证协议使用不依赖JSON顺序的长度前缀消息，覆盖全部已知任务字段，并冻结跨连接器测试向量；
- Bridge认证入口实现HMAC-SHA-256、5分钟时间窗、未知/吊销密钥、原子nonce接口和重放拒绝；
- Credential Manager读侧只解析`Yuanyuan/TaskEventKey/<key_id>`；正式写侧生成32字节随机密钥、拒绝覆盖已有目标、生产固定使用本机后续登录会话可见的持久化级别，并且只删除自身生成的精确目标；
- 新增独立`connector-trust.sqlite3`：每个`connector_id + source_instance`绑定独立随机`key_id`，稳定核心唯一写入，Bridge/AI只读；活动密钥、切换前旧事件最长5分钟宽限、切换后旧签拒绝、立即重置和历史`key_id`永久禁用均由事务状态机执行；
- 凭据与信任提交采用安全顺序：新凭据先创建、信任事务失败则精确清理；撤销/宽限到期先移除权限再删凭据，删除失败只留下无认证权限的孤立凭据；
- 信任审计只有固定动作、固定原因、时间、身份和`key_id`，没有密钥/载荷/错误正文；旧开发表自动迁移为固定`legacy_migration`原因；身份容量上限16；
- 稳定核心新增连接器信任只读状态、两分钟单次确认令牌和创建/轮换/重置/重连命令；只接受内置Codex/Claude Code及UUID实例、最多16个待确认操作，不返回`key_id`，并明确`hookConfigurationChanged=false`；
- 稳定核心新增Codex/Claude Code只读安装发现：只检查普通可执行文件、Codex桌面应用注册/管理位置和Claude官方原生用户安装位置，不启动来源程序、不读取配置/会话/项目/任务数据、不返回绝对路径；桌面应用内置命令不会重复误报为独立CLI；
- 被动发现不创建身份；首次授权预览才由稳定核心生成随机UUID，确认应用后随信任记录持久化。脱敏发现状态把安装、授权、Hook和事件健康分开，信任库损坏/不可读显示为不可用而不是未配置；
- 新增独立`connector-authentication-health.sqlite3`：同一已授权身份在10分钟内连续3次可归属认证失败后暂停，未知身份/密钥不能填满计数；HMAC成功重置未暂停序列，已暂停状态只能由成功授权变更清除；库中没有密钥、载荷、路径、错误正文或任务内容；
- Bridge在暂停后不读取密钥、不投递或暂存事件，只记录固定`authentication_paused`诊断；无效认证、凭据丢失与暂停路径继续保持空输出和退出码0，不改变来源任务结果；真实CLI覆盖活动信任下凭据丢失前三次计数、第三次暂停和第四次直接拒收；
- Bridge原始来源签名前先验证当前活动密钥与连接器身份，Bridge/AI验签再次绑定身份、签名时间和当前时间；普通Credential Manager读取不能绕过生产验签的信任授权；
- 真实Windows凭据夹具覆盖正式随机建钥—轮换—宽限清理—立即重置闭环；独立交互式发布门验证生产级本机持久化凭据可创建、读取并精确删除；
- SQLite nonce表通过主键、立即事务、重启和两个并发连接测试，并在插入前清理过期记录；
- 命名管道服务端使用当前用户SID的受保护DACL和`PIPE_REJECT_REMOTE_CLIENTS`，同一用户安全往返已通过；
- Windows线程切换为真实匿名令牌后，内核以`ACCESS_DENIED`拒绝打开当前用户专属管道；测试使用RAII恢复原身份，远程客户端仍由`PIPE_REJECT_REMOTE_CLIENTS`拒绝；
- 暂存帧冻结schema版本、长度、CRC32C和落盘时间，并实现原子改名、SHA-256去重、跨进程锁、文件/字节硬上限、隔离区轮换与确认式有序重放；
- 只有HMAC预验证成功的事件能进入暂存，预验证不消费nonce，最终接收后才登记nonce并删除文件；
- 最终CLI直接发送调用方提供的原始已签名字节，只有管道传输失败时才惰性打开暂存，成功路径不创建暂存目录；
- CLI在创建空暂存根目录后、创建任何队列文件前应用当前用户专用DACL，并将同一权限边界应用到`pending`、`quarantine`和锁文件；
- CLI的认证、传输和暂存整体受3秒进程级看门狗约束，测试中的阻塞工作线程不会延长Hook关键路径；
- 真实`yuanyuan-bridge.exe`成功路径已用Credential Manager中的受控密钥签名：服务端收到完全一致的原始字节并在ACK后令CLI静默成功退出，成功路径不创建暂存目录；
- 管道已升级为双向消息通道；客户端只有精确收到固定8字节`YYACK001`才视为送达，错误ACK或接收端提前断开都会触发失败路径；
- 服务端以`ReceivedPipeEvent`显式分离“收到字节”和“确认提交”，调用方完成认证、nonce登记与持久化后才应调用`acknowledge()`；
- 新增独立`yuanyuan-ai` crate和`yuanyuan-ai.sqlite3` schema；任务、事件和nonce不进入`yuanyuan-reminder.sqlite3`；
- AI任务库在单个`BEGIN IMMEDIATE`事务中登记nonce、保存事件并更新当前任务；任一写入失败会整体回滚，同一已提交事件在ACK丢失后可幂等确认；
- 任务归组阻断活动运行被另一`run_id`替换和已生效旧运行回流；父关系允许迟到补全、后续省略时保留，同一运行改挂会保留拒绝审计但不改变摘要。父子乱序到达、跨工作区隔离、提前拒绝后合法新运行和终态纠正均有数据库级回归；
- AI库schema v4为当前任务摘要增加固定证据等级；既有v3任务迁移为`unknown`且失败保持v3完整。`waiting_user/succeeded/failed/cancelled`只有权威证据进入只读表达，Codex部分完成事件贯穿连接器、签名、数据库和表达读取后只能成为`unknown`；非权威终态不会锁死后续继续运行或权威确认；
- 稳定核心新增返回原任务工具的静态安全门禁：仅识别带随机实例身份的内置Codex/Claude Code连接器与精确`open.task`动作，目标只允许1—128字节ASCII不透明标识；渲染阶段签发2分钟、最多64项的一次性UUID凭证，原始目标不进入前端，点击后再按当前注册表、完整来源绑定和域分隔摘要复核。文件、URI、Shell、PowerShell、脚本、路径、控制字符、非ASCII、过期、重放、撤销和篡改全部关闭失败；真实来源处理器未过门前固定降级任务守望台，模块无Tauri命令或正式按钮；
- 主提醒库schema v11新增脱敏任务守望暂缓表，只保存Codex/Claude Code、五种固定可暂缓状态、截止与更新时间；正式任务台提供“10分钟后再提醒/恢复提醒”，任务聚合状态始终可见且来源任务不受修改。稳定核心只抑制匹配来源/状态的圆圆主动表达，到期非终态恢复，到期失败逐来源聚合一次；专注、安静、基础陪伴和本地提醒阻止呈现时不消费，表达成功后再按原截止时间确认，避免并发续期被旧确认误删；
- AI任务库新增`SQLITE_FULL`和并发写锁故障注入：失败事件的nonce、事件和任务更新均为零写入；解除容量限制或释放写锁后，同一事件可以正常重试提交；
- AI任务库以只读句柄打开时拒绝写入且不消费事件，恢复可写后可提交同一事件；数据库文件被独占时启动干净失败，释放独占后可重新打开；
- `YuanyuanAI.exe`原型启动时先重放受限暂存，再持续监听安全管道；合法事件提交成功后才ACK，无效事件无ACK且数据库零写入；
- AI服务使用独立关闭管道唤醒阻塞中的任务等待并优雅退出；当前用户文件锁拒绝第二实例，连续多事件测试通过；
- AI控制管道新增版本化健康探测；稳定核心只从自身目录解析固定AI程序名，健康通过后才标记运行；
- AI控制协议冻结为v1；核心与AI的版本区间必须有交集，过旧、过新和无效区间均关闭失败，不会把未知协议误判为健康；
- 稳定核心监督线程实现正常关闭、3秒启动健康门、1/2秒指数退避和5分钟内第三次失败熔断；AI程序缺失时保持`Unavailable`，不影响提醒核心；
- 熔断后不自动恢复；仅在状态为`CircuitOpen`时接受显式重试，开启新的监督周期。后端已提供状态查询和手动重试入口，浏览器演示环境固定返回`Unavailable`/`false`，不会伪装AI已安装；
- 新增真实`yuanyuan-ai.exe`调试子进程矩阵：v1进程拒绝v2核心探测，v2夹具进程拒绝v1核心探测；错误版本连接后，正确版本仍可重新健康确认并优雅退出；
- 设置页新增本地诊断卡片：检测到AI二进制时提供“只读预览→选择保存位置”，预览显示唯一JSON、预计大小、队列/固定错误统计、明确排除字段和独立扫描v1的4项通过结论；Windows原生窗口只允许用户选择本机普通驱动器上的`.json`，取消不创建文件且保留预览，既有目标不覆盖，绝对路径不回传前端，也不创建应用内副本或自动上传。熔断状态说明提醒不受影响，并提供显式“重新检查”。AI移除后若仍有诊断残留，卡片只保留二次确认清理入口；无组件且无残留时不显示。预览与清理二次确认会把焦点送入主操作，并在取消后返回原入口；状态变化、忙碌状态和强制颜色边界均有程序化语义；
- 设置页新增只读“任务守望连接器”卡片：分别显示安装渠道、授权状态、Hook未知和事件健康；认证暂停明确说明只暂停圆圆接收且来源任务不受影响，实例UUID不进入可见文案；除重新检查外，只提供不签发确认令牌的未来授权范围说明，明确Bridge未进入正式包前不开放执行；
- 新增独立`yuanyuan-connectors` crate，将Codex `notify`/Hooks和Claude Code Hooks载荷保守转换为统一事件，不负责安装或修改真实Hook配置；
- 复核并冻结2026-08-04官方Hook外形：Codex支持独立`hooks.json`和`config.toml`内联`[hooks]`且来源叠加，Claude Code使用各级`settings*.json`中的单一`hooks`对象且来源叠加；真实目标版本、载荷与实机信任仍为发布门；
- 连接器crate新增64 KiB只读Hook配置预览：同时理解Codex JSON/TOML与Claude设置JSON，按`owner_id + connector_id + source_instance`区分缺失、完全一致、被修改和重复，只返回脱敏数量/状态，不返回命令、路径、身份或凭据引用；当前明确禁止落盘；
- 只读预览扩展为最多32个预期handler的一次解析：集合级区分完整、部分缺失与冲突，同事件多matcher可确定性归属；混合组分别统计用户/圆圆handler，无法归属的圆圆条目停止猜测；
- 多来源预览冻结首选用户级来源并阻断Codex内联TOML/JSON双表示、圆圆条目位于非首选来源或跨来源重复；Claude项目级只有用户Hook时允许共存，所有输出仍只有固定枚举和数量；
- 固定 `builtin.codex` / `builtin.claude-code` 已与持久化实例身份分离；新注册生成带随机 UUID 的稳定 `connector_id` 和独立随机 `source_instance`，历史固定身份只读显示为需要重连，不会进入未来 Hook；
- 设置页只对有效随机实例开放显式“只读检查 Hook 配置”：安全定位 Codex/Claude 用户级目录，拒绝相对根目录、重解析点、非普通文件和 64 KiB 超限输入；只返回脱敏计数与冲突枚举，不读取项目/任务正文、不启动来源工具且零写入；
- 连接器纯库新增 JSON/JSONC 无损 Hook 差异编辑器：只在安全的缺失/部分安装状态下插入缺失组，逐字保留旧配置、注释、未知字段和用户 Hook，生成后再次验证完整所有权集合；重复键、修改/重复圆圆条目、TOML 和输出超限全部关闭失败，当前仍只生成内存候选、零落盘；
- 稳定核心新增私有 Hook 写入协调器原型：两分钟 UUID 单次令牌绑定目标存在性、原始/候选 SHA-256、Windows 文件身份与 handler 数量；确认后重读并重建候选，现有文件先创建同目录当前用户专用时间戳备份再写穿透原子替换，缺失文件不覆盖并发新文件；只按本次创建的文件身份清理或回滚。底层模块不自行解析官方路径，也没有 Tauri 命令或设置页入口；
- 新增私有官方目标封装：生产预览只接收已授权随机连接器身份，确认只接收令牌，调用方不能传路径；核心重新读取授权和当前 Bridge 参数，从 `CODEX_HOME` / `USERPROFILE` 解析唯一用户级根目录，并把 Codex `hooks.json + config.toml` 或 Claude `settings.json` 的存在性、摘要与文件身份整体绑定。任一非首选来源、授权材料或根目录在确认期间变化都会消费令牌并停止；模块仍无 Tauri 命令和前端入口；
- 连接器纯库与私有事务新增精确撤销：只删除语义所有权和规范原始节点字节同时匹配的圆圆handler；规范单-handler组可整组移除，混合组保留用户handler、注释和未知字段；部分安装与重复撤销幂等，圆圆节点被加注释、重格式化、修改、复制或跨来源变化均停止。官方断开入口同样只接收授权身份/单次令牌，连接—断开闭环使用精确备份和原子替换；
- 稳定核心新增私有断开组合状态机：默认顺序为精确配置撤销成功后再立即重置信任；配置冲突不签发组合令牌，但明确允许另行预览“仅撤销信任”；配置执行失败不尝试断权，配置成功后断权仍活动/不可确认可独立重试；SQLite权限已撤销但凭据清理失败不会误报为仍有权限，本地DACL维护失败及两者同时失败也分别分类。组合令牌两分钟单次、最多16项，输出无身份/路径，且无Tauri命令或正式入口；
- 信任库为已撤销密钥增加独立凭据清理完成标记；失败后维护事务只处理该实例中已撤销且未标记完成的精确key id，每次最多256项，成功/已不存在后记录推进，重复调用幂等。维护使用与断开动作隔离的两分钟单次令牌，与断开共享16项容量；活动权限、错误动作、跨入口令牌、过期和重放全部关闭失败，不读取Hook、不扫描Credential Manager且不重新授权；
- 新增仅开发环境动态加载的断开/恢复交互实验台：两种模式分别说明配置与权限影响，配置冲突只允许明确改选仅断权，模式变化清除旧勾选；执行顺序、配置失败、权限仍活动、状态未知、凭据/DACL维护待完成和固定恢复动作分别展示。组件默认关闭执行，1100×800与390×620浏览器实拍无横向溢出，读屏角色可区分状态/强警告，控制台无warning/error；正式命令仍未注册；
- Bridge接受可选且只能等于`yuanyuan-reminder`的`--owner-id`，为未来新Hook提供不扩展第三方配置Schema的所有权标记；旧调试调用暂时保持兼容；
- Claude事件映射按当前官方Schema收紧：`TaskCompleted`不读取不存在的状态字段，合成`failed/cancelled`不能虚构终态；`StopFailure`只形成部分失败证据并丢弃错误正文，普通取消仍未建立权威映射；
- 连接器输入上限64 KiB，提示词、回复、工具输入输出、终端记录和原始工作目录全部丢弃，外部任务/会话标识只保存SHA-256派生的不透明ID；
- 未知来源版本不能生成终态，`Stop`/`StopFailure`保持未知，单次工具失败保持任务运行；固定Hook退出契约为空stdout和退出码0；
- 建立14份带来源、语义、证据等级和敏感字段元数据的合成契约夹具，覆盖核心状态、重复、乱序、缺字段、额外字段和未知版本；夹具明确标记为非真实录制，不能替代发布证据；
- 对本机官方Codex桌面包26.727.6591.0的捆绑程序进行只读静态检查，确认`agent-turn-complete`及开始、提示提交、工具、权限、子任务、停止等事件词汇存在；该证据不证明载荷字段与终态语义；
- Bridge新增原始来源模式：从stdin读取Codex/Claude载荷，经连接器转换后从Credential Manager取钥、生成随机nonce并签名，再复用现有安全管道、ACK和受限暂存路径；
- 真实Bridge调试进程已完成Codex成功和Claude失败两条端到端夹具，接收端验证签名与状态，确认敏感内容及原始任务/会话ID未进入投递载荷；缺失凭据路径在1秒目标内静默fail-open；
- 新增非语言任务表达导演，将running、long-running、waiting_user、succeeded、failed、stalled、cancelled和unknown确定性映射到现有动画、固定道具、标签、注意方式和可访问名称；
- 表达导演实现纯动作、自适应、始终标签三档；专注期间除waiting_user外不主动推出道具，减少动态不改变语义；
- 新增仅开发环境可进入的表达实验台，不使用猫咪对白气泡；已实际检查宽屏和390×620单列布局、状态切换、标签隐藏与专注降级，控制台无warning/error；
- 新增`response_intent` v1：八种非语言语义意图与四级优先级不携带台词、动画名或工具参数，未知版本/意图确定性降级为安静存在；
- 新增Rust稳定核心表达导演v1：输入只有固定提醒、任务、用户回应和睡眠信号，模型优先级被限制在用户回应层内；输出只有N0—N4、固定姿态/道具/标签/注意/可访问状态，没有标题、消息、台词、路径、动画名或资源参数。喝水、到期事项、活动、专注、睡眠和减少动态已接入生产状态，解除高优先级信号后从仍有效集合恢复且不残留道具；正式宠物窗口启动读取并持续消费核心快照，本地提醒动画不再自行决定姿态；
- 稳定核心新增AI任务库只读表达摘要：每15秒只读 `task_key/source/state/evidence_level/updated_at_unix_ms` 五列，最多64项，缺失库不创建、旧Schema不迁移、异常固定字段或目录/链接/重解析路径关闭失败并清除旧状态。等待用户、30秒终态、10分钟长运行和24小时失效窗口已接入表达导演；非权威决定性状态先降为状态不明，标题、正文、工作区和原始事件均不进入快照；
- 提醒库Schema v8新增终态聚合预算，Schema v9新增主动陪伴额度，Schema v10将固定主动事件扩展为专注结束与重逢，并覆盖v9→v10保留数据升级：非紧急终态在30秒窗口合并，最多每10分钟呈现一次并跨重启去重；两种主动仪式共用安静0次、日常3次/日且1次/小时、亲近6次/日且2次/小时三档额度。预算表只保存固定事件种类、来源/结果枚举、数量和时间，不保存任务身份、离开原因、活动内容、正文、工作区或提示词；故障注入证明失败写入整体回滚；
- 专注完成不再发送“辛苦啦”等猫咪气泡，改为受预算控制的N1伸懒腰动作；休息结束仅保留系统到时通知。夜间安静时段、自动睡眠、一键暂停、专注、待处理提醒、`waiting_user`及更高优先级状态均在扣减预算前阻止主动仪式；固定无障碍文本提供动作等价语义；
- 新增离开后重逢仪式：只有白天因真实闲置开始自动休眠、离开峰值达到10分钟且没有跨入安静时段，唤醒后才尝试N1靠近轻蹭动作；资格和时长只存在当前进程内存。夜间安静、短暂离开、手动睡眠/唤醒、专注、提醒、基础陪伴或其他主动仪式均在扣减额度前阻止，宠物窗口不显示对白、标签或道具；
- 宠物窗口新增首版固定道具层：小电脑、铃铛、任务牌、任务篮、提词器和系统确认卡只使用固定短标签/来源枚举/有界数量，支持前移、收纳、一次铃动与减少动态；外部任务无需猫咪对白气泡即可通过姿态和实体道具表达。外部任务道具已成为明确可聚焦控件，可打开任务守望台；本地提醒道具仍不可点击，避免把两类含义混在一起；
- 新增只读任务守望快照和生产页面：最多沿用64项、仅保留最近24小时状态，在Rust端聚合为来源、固定状态和数量后才交给前端，不返回标题、正文、工作区、任务标识或精确时间；状态库缺失时入口不出现，读取失败只影响该页。开发固定状态夹具受`DEV`编译门保护，发布边界扫描禁止其查询标记进入`dist`。390×620与520×800浏览器检查无横向溢出；另以非默认`runtime-qa`功能、带标记隔离目录和8条合成认证事件贯通真实Tauri猫咪道具→任务守望台，当前150%缩放下窗口无裁切、真实Tab顺序和道具点击唤起面板通过，正式用户目录新增写入为0。夹具不进入默认安装包，且不代表真实Hook，见`P0_TASK_WATCH_CENTER_QA_2026-08-05.md`；
- 生产PetIntent已冻结文字表面：只有绑定真实事项的喝水/事项/活动事务进入系统信息卡；专注倒计时和玩具说明分别进入计时卡、工具卡；喝水成功、延后、跳过、喂食和宠物喝水的载荷标题/消息为空，只保留动作。固定无障碍语义不读取事件文字；生产CSS/DOM移除旧气泡类名，发布边界会拒绝旧类名和历史猫咪对白回归；
- 新增`display_document` v1及Rust/React双层允许列表：只支持标题、段落、列表、表格、代码、展示型引用和四种固定动作；Markdown、HTML、脚本URL、远程资源与正文伪按钮保持字面文本，未知块、错目标、双向控制符和超限内容关闭失败；
- 新增厂商无关Provider Adapter契约和有界事件通道，宿主拥有结构化终稿校验、取消、100毫秒至120秒看门狗、用量能力核对和终稿唯一性；回环夹具已覆盖成功、取消、超时、畸形/重复终稿与虚假能力；
- 新增中文FTS5检索Spike：6条合成场景中`trigram` Recall@3为6/6，未分词`unicode61`为0/6；“焦虑”“加班”采用参数化、转义且限制结果数的两字字面回退，查询运算符不能注入；
- AI数据库新增v2原子迁移和`ai_schema_meta`、精选记忆、来源/冲突/替代关系、检索使用记录、删除墓碑与正式FTS表；v1任务数据升级保持不变，v2建表冲突会完整回滚并保持v1可重试状态；
- 低信任模型推断、系统观察和外部内容只能形成待审核草稿；用户采纳后生成独立`user_confirmed`记录并保留来源链，恢复数据即使伪装成`active/high`也不能绕过检索和高置信事实资格；
- 冲突双方在解决前退出检索，过期记忆移出索引；彻底删除递归清理派生/重复/替代谱系的正文、FTS、检索审计和关系，并为每个身份写墓碑，墓碑故障时整项删除回滚；
- 当前墓碑可先应用到旧备份的临时恢复候选，自动清理静默复活的旧谱系；ADR-0009冻结DPAPI本机备份和口令便携导出的不同密钥模型、恢复顺序与重新授权边界，正式加密实现仍保持关闭；
- 新增用户主动触发的安抚状态机；完整六路径仅在专业复核和地区资源同时就绪时开放，否则确定性裁剪为“只陪我一会、陪我动一动、先别管我”，AI和数据库不可用不影响三路径；
- “只听不记”正文只存在于当前前端会话内存，最大16 KiB，拒绝NUL/双向控制字符；完成、关闭和新会话会断开唯一应用层引用，源码边界测试同时禁止浏览器存储、数据库调用、日志、网络和后端命令。该边界不声称取证级内存清零；
- AI数据库当前迁移到v4：v3只新增用户确认后的类型化支持偏好，v4只增加任务摘要固定证据等级；不存在倾诉原文、原因或自由文本列。支持全局/项目隔离、删除墓碑和旧备份防复活；v3或v4迁移失败分别保留完整上一版本数据库；
- 地区安全资源包v1冻结64 KiB上限、字段、HTTPS来源、最长180天复核周期和必需紧急服务；缺失、非法、未来生效或过期全部进入无电话号码的固定通用面板，当前测试只使用虚构占位资源；
- 新增仅开发环境可进入的三路径基础陪伴实验台，以猫咪动作、软垫/毛线球/留白牌表达陪坐、活动和后退；不含猫咪对白气泡、倾诉、呼吸、模型或疗效表述，1100×800与390×620验收无横向溢出且“先别管我”无回看；
- 三路径基础陪伴已从开发实验台接入正式照料页和 Rust 稳定核心：用户主动选择固定路径与固定时长，主进程内存会话支持自动/手动结束，应用重启不恢复；数据库无支持/情绪历史表，不提交原因、情绪或自由文本；喝水、到期工作事项和等待用户可抢占，活动、任务终态和后台守望不打断，专注与普通猫咪互动会结束本次陪伴；
- 正式 release 主程序已完成首轮真实Tauri窗口检查：逻辑390×620面板的DPI感知抓取为607×943像素，今日页、互动页、三路径选择器和5/15/30/60分钟选项均无横向裁切；1分钟会话自动结束、5分钟会话手动结束、宠物纯动作和“先别管我”退开后停留均取得窗口级截图证据，见 `P0_SUPPORT_RUNTIME_QA_2026-08-05.md`；
- 三路径选择器新增确定性键盘焦点管理：打开后进入第一张小牌，取消和手动结束后返回入口，开始后进入结束按钮；基础陪伴所有按钮具有明确 `:focus-visible` 焦点环。DOM回归覆盖这四条路径，但真实Windows键盘注入仍受测试会话限制，不能替代人工键盘验收；
- 面板和宠物信息卡新增Windows强制颜色适配：使用`Canvas`、`ButtonFace`、`Highlight`等系统颜色保留边界、选中态、禁用态和焦点环；20文件/115项前端回归中包含静态样式门。该证据只证明生产CSS包含确定性适配，不替代真实Windows高对比度模式实拍与读屏验收；
- 正式道具层新增“纯动作 / 需要时显示 / 始终显示”三档持久设置并默认“需要时显示”：适应模式只为等待用户、失败、疑似停住和状态不明显示固定中性短标签，纯动作不生成备用文字，来源标识和固定读屏语义不随视觉标签关闭；旧设置通过JSON默认补全为适应模式，无需新增数据库迁移，Rust验证拒绝未知模式。被动的专注结束和重逢自然结束不计用户忽略，当前不建立邀请忽略历史；
- 三档标签正式设置页已完成当前144 DPI（150%）环境的真实Tauri验收：DPI感知窗口607×943内七项导航和设置项完整可见；默认“需要时显示”、临时切换“纯动作”、精确进程重启后保持及恢复原设置均取得窗口截图。测试不接入Hook、不制造外部任务，见`P0_LABEL_MODE_RUNTIME_QA_2026-08-05.md`；
- 设置行新增程序化标题和说明关系，开关继承固定名称，安静/喝水/活动时间输入分别提供开始与结束名称。最新release的Windows UI Automation已展开WebView内部树：道具标签为可聚焦组合框且值为“需要时显示”，看向鼠标支持TogglePattern，设置按钮后的前五步Tab顺序包含道具标签；正式包验收期间发现并修复“WebView先发命令、AppState后注册”的初始化竞态，核心状态现于配置窗口创建前完整注册，精确构建完成1轮独立加3轮连续启动/单实例/完全退出回归，退出码均为0且独立时间窗无新增崩溃事件。该证据仍不替代Narrator人工听读和完整端到端键盘验收；
- 三个开发实验台改为仅在开发环境动态加载；生产边界脚本扫描`dist`中的代码、文案和样式，并核对Tauri `externalBin/resources`，防止实验台或Bridge/AI原型静默进入正式包；
- P0-E08发布门已开始落地：版本化策略保持渠道和发布者未冻结，不为未知主体猜测资格；PowerShell采集五项产物Authenticode、精确Subject、证书指纹和时间戳事实，Node预检重新核对候选集合、角色、SHA-256、安装边界、同证书和候选绑定人工证据。普通预检生成报告，严格门在任一待办项存在时退出2；
- 稳定核心新增AI辅助进程签名启动门：共享Windows离线WinTrust适配器在验证链成功后提取精确发布者和签名证书DER的SHA-256；主程序与`yuanyuan-ai.exe`必须位于同一目录、使用固定文件名、同时匹配编译期冻结的发布者与证书。验证期间取得的两个只读文件句柄在整个监督周期保留且不共享写入/删除，阻止验签后替换。版本化策略当前明确`enabled=false`，因此即使开发构建目录旁存在未签名AI原型，正式监督入口也保持`Unavailable`；5项策略/身份/锁定回归通过，真实签名正向样本和包身份仍待发布主体与渠道冻结，见`P0_AI_SIDECAR_TRUST_GATE_2026-08-07.md`；
- Windows x64 CycloneDX 1.6与许可证清单生成通过：507个去重组件中306个为发布/构建范围、201个为锁定开发范围，0项许可证声明缺失、26种总体表达式。新增2,686,481 bytes确定性生产依赖全文归档，覆盖301个第三方生产组件、22种精确生产表达式、11个受来源约束的回退映射和5个精确MPL源码地址；项目、素材、摘要和逐组件全文四份材料已由Tauri资源映射进入NSIS的安装与卸载脚本。自动证据不作法律结论，素材权利、回退选择、MPL可得性及发布主体/地区仍待人工复核。Defender扫描脚本要求服务真实启用并绑定便携主程序、NSIS实际安装主程序和安装包三项哈希；当前机器由火绒接管安全产品状态，`WinDefend`停止且`Get-MpComputerStatus`不可用，未修改用户安全服务，也未生成虚假通过报告；
- 许可证人工门由裸布尔值升级为候选绑定契约：8,938 bytes复核包冻结三项正式产物、八份许可/策略材料、507/306/301组件计数、22种生产表达式、11个回退映射与5个MPL源码地址；3项契约测试证明具名人工正向签字可通过，AI/自动化签字、未完成决定、渠道/候选漂移、未解决发现和材料包漂移关闭失败。当前只存在空模板，没有实际签字文件，`licenseReviewVerified=false`且预检保持`license_review=pending`；
- 无障碍体验门不再依赖零散文档声明：3,762 bytes候选绑定包冻结便携主程序、NSIS实际安装主程序与安装包，强制最终签名候选上的100%/125%/150%/200% DPI、100%/150%/200%文本缩放、深浅对比度主题、系统与应用减少动态、五条全键盘路径、六条Narrator实际听读路径及安装/默认保留/显式删除卸载矩阵。3项契约测试接受完整具名人工正向链，并拒绝AI测试人、自动化主要结论、缺档矩阵、Narrator未听到、未签名候选和裸布尔伪通过；当前只存在关闭失败模板，`accessibilityAcceptanceVerified=false`且预检保持`accessibility_acceptance=pending`；
- 发布产物清单v1现为稳定/便携主程序、NSIS实际安装主程序、Bridge原型、AI原型和NSIS安装包5项固定角色、字节数与SHA-256。新增一次性静默安装—提取—卸载门，证明Tauri包类型标记使构建目录`UNK`主程序与NSIS内`NSS`主程序只差3 bytes但哈希不同；实际安装变体已独立进入清单、字节边界、AI关闭、签名同一性和Defender目标。清单仍明确`signatureVerification=not_performed`，不能替代后续Authenticode与时间戳门，详见`P0_NSIS_INSTALLED_PAYLOAD_QA_2026-08-09.md`；
- P0-E08新增候选绑定的隔离安装器转换探测：冻结官方v1.3.2 Setup与实际安装主程序身份，在带所有权标记的一次性目录执行1.3.2→1.4.0→卸载当前版→回装1.3.2；三个安装阶段的版本/字节/SHA-256精确匹配，v1.4.0四份许可证名称与内容正确，回装旧版后无新许可证残留，正式LocalAppData中的合成哨兵五阶段不变，最终注册、快捷方式、数据、临时目录和进程零残留。当前账户的自定义安装根未暴露每用户控制面板注册，故自动门只记`isolated_installer_transition_probe`通过，默认路径注册、真实旧库、文件替换中断和签名候选仍留在完整人工门，见`P0_RELEASE_UPGRADE_ROLLBACK_PROBE_2026-08-09.md`；
- 同一转换探测新增必须明确确认干净测试账户的默认安装路径模式：不传`/D`的官方v1.3.2→当前候选→卸载当前版→回装旧版文件转换、许可证清理、正式数据目录合成哨兵和零残留均通过，预检独立记`default_install_path_transition_probe`。当前受管账户对随机非产品HKCU测试键的父进程写入直接返回访问拒绝，三个安装阶段也均观测到`registrationDisposition=absent`，因此报告保持`registrationGatePassed=false`；预检已新增独立`default_install_control_panel_registration=pending`，控制面板注册不会被文件结果冒充通过，仍待允许current-user注册写入的外部干净账户，见`P0_RELEASE_DEFAULT_INSTALL_PATH_QA_2026-08-09.md`；
- 新增候选绑定的NSIS卸载数据选择探测：默认/静默卸载返回0并保留LocalAppData与RoamingAppData合成哨兵；交互轮精确找到真实`Delete the application data`复选框，证明其初始关闭、明确设为开启后再调用卸载与完成按钮，两处数据根和安装目录均删除，进程及隔离状态零残留。报告只使用合成数据，按候选、实际安装主程序和脚本哈希进入独立`uninstall_data_choice_probe`，见`P0_RELEASE_UNINSTALL_DATA_CHOICE_QA_2026-08-09.md`；
- 新增脱敏诊断快照v1：Schema只有版本、时间、协议、固定AI状态、固定诊断码计数和队列规模，没有任务/路径/提示词/倾诉/凭据字段；单文件64 KiB，独立扫描器只接受可往返为相同规范字节的精确Schema并拒绝敏感标记。用户选择导出使用同目录临时文件、当前用户DACL、同步落盘、无覆盖原子改名及临时/最终两次复读扫描；只接受本机普通驱动器上的绝对`.json`且拒绝重解析祖先。旧的应用自有目录仍保持最多5份和匿名Windows身份读取拒绝，当前导出不在其中创建副本；真实监督状态、只读队列统计和固定错误码本地计数已接入用户主动入口；计数锁冲突零等待、失败不影响Hook；
- 脱敏诊断卡已在当前144 DPI（150%）桌面完成真实Tauri正向验收：DPI感知窗口607×943，逻辑404.7×628.7，初始卡与保存前预览均无裁切；真实Enter/Tab焦点链为“查看→选择保存位置→取消→返回查看”，第二轮真实打开并关闭Windows原生保存窗口。隔离位置只生成一份434字节合成JSON，独立复验确认Schema、固定计数、敏感匹配0、扫描v1的4项检查、路径不回传、无应用内副本和无自动上传。采集使用预启动中性隐私背景，正式目录新增写入、Application Error均为0，应用/背景受控退出且隔离根清理完成；两张截图人工复核无桌面内容。schema v2报告绑定主程序、夹具及采集/背景脚本，独立校验器会拒绝DPI虚拟化、越界控件、来源漂移、灰屏/错尺寸截图和敏感/陈旧/乐观导出，7项篡改回归通过。100%、125%、200% DPI与Narrator人工听读仍待完成，见`P0_DIAGNOSTIC_CARD_RUNTIME_QA_2026-08-09.md`；
- 新增任务事件有界清理器：普通进度7天、等待用户/终态/纠正证据90天、终态任务摘要180天、无更新非终态摘要365天；每轮最多1,000事件/250任务，最新事件豁免，事件/任务/过期nonce在同一立即事务中故障回滚；常驻服务在双管道健康后延迟5秒执行首轮并每6小时低频重复，优雅退出会立即停止并回收维护线程，单轮失败不影响健康且不取消下一轮。全新库启用有界增量回收、每删除周期最多256页，旧库和未知v0表不隐式转换。真实子进程与一年等价负载均通过，见`P0_TASK_RETENTION_SCHEDULER_2026-08-06.md`和`P0_TASK_RETENTION_LOAD_QA_2026-08-06.md`；
- 真实Windows子进程夹具证明：连续三个立即崩溃的子进程会进入熔断；长驻但不提供健康协议的子进程会在健康超时后被终止；
- 已提交事件写入真实AI数据库后，强制终止运行中的`yuanyuan-ai.exe`不会丢失记录；同一数据库可由新进程再次健康启动并优雅关闭；
- 真实崩溃循环期间，提醒库的设置读取、今日事项查询和专注启动持续成功，AI故障未锁定或污染`yuanyuan-reminder.sqlite3`；
- 连接器信任生命周期协议已冻结于`docs/protocol/CONNECTOR_TRUST_LIFECYCLE_V1.md`；辅助进程仍未接入安装包，真实Hook配置仍保持未修改。
- 项目级Hook只读检查已接入正式任务面板和Tauri官方原生目录选择器：命令只接受连接器身份且仅允许`panel`窗口调用，路径只在Rust选择器适配器内出现并立即密封；来源工具双因子和授权在选择前、选择后及确认后重复校验。页面只收到两分钟单次令牌与固定隐私布尔值，取消立即销毁令牌；确认后仅合并固定用户级及所选项目`.codex`/`.claude`Hook来源，不读取代码、任务、对话、Git或递归内容，不返回/保存路径且零写入。原有同名替换、配置子目录替换、重解析目录、容量和脱敏保护继续生效；真实Hook写入仍无产品入口。
- 来源工具信任复核已升级为双因子：安装发现返回独立`toolTrust`状态；单一普通EXE必须先通过Windows离线Authenticode链、精确发布者和冻结分发位置，再通过对应的离线分发身份。Codex现用系统`GetStagedPackagePathByFullName`交叉确认固定包族、发布者ID、架构、系统暂存根和包内程序位置，本机真实Store管理Codex已通过；Claude现用v2编译期证据包与打开文件SHA-256交叉，证据同时绑定规范版本、平台、清单SHA-256、二进制SHA-256和代码审核过的签名根版本区间。生成器对清单、签名和keyring先做稳定文件快照，只接受唯一有效签名并识别签名子密钥，拒绝撤销/过期/多个签名、未知根、重复条目、未知字段、非规范哈希及校验器替换；新根不能由命令行放行，必须同步修改生成器与Rust版本策略。当前证据包已导入Claude 2.1.211的x64/arm64真实清单条目，真实x64文件在不运行Claude的情况下通过Windows签名和证据摘要双因子适配器；同名脚本、普通Codex PATH CLI、多安装冲突、签名/发布者/包身份不符、未收录Claude版本、验证器不可用和校验中同名替换全部保守降级。不启动来源进程、不联网且不返回路径、哈希或证书材料。
- Windows来源工具信任复核进一步加固本机证书读取：只关闭非空WinTrust状态，以系统辅助函数取得并检查首个证书项，并限制证书显示名称缓冲区；共享适配器加入签名证书DER SHA-256提取后，本机受管理Codex的8线程签名与包身份适配测试再次连续20轮、共160次通过。历史两次间歇性`STATUS_ACCESS_VIOLATION`因缺少转储仍不能归因或关闭，压力证据与剩余风险见`P0_WINDOWS_TEST_STABILITY_QA_2026-08-07.md`。
- 任务“没成功”的动作边界已从可循环失败姿态收紧为一次性约1.6秒察觉动作，随后在任务牌仍可见时进入`focus-calm`安静陪伴；减少动态直接安静陪伴。隔离`task-failure-motion`运行配置以单一权威失败事件、Windows UI Automation固定语义检查、预启动中性隐私背景、4秒连续采帧、受控退出和带标记目录安全清理关闭失败。当前150% DPI真实Tauri窗口中，完整动态24帧有16个不同帧，减少动态24帧只有1个不同帧；两种模式的应用/背景退出和隔离清理全部通过，人工复核确认一次察觉后安静收束且失败牌持续。单环境连续播放门已关闭，多DPI、高对比度、Narrator、真实签名Hook和签名候选仍开放，见`P1_TASK_FAILURE_MOTION_RUNTIME_QA_2026-08-07.md`。
- 生产动画选择不再仅凭共享`StayClose`姿态判断失败：只有`task_failed`播放一次失败察觉，长运行、基础陪伴和普通陪在身边均保持安静。需要用户和检查/收起任务牌也从无限循环收紧为一次提示，之后道具不消失并回到`waiting`/`focus-calm`；减少动态直接使用安静姿态。1280×720开发实验台已实测起止、固定无障碍状态、减少动态、零错误和零横向溢出；隔离夹具新增等待用户/可能停住场景并取得关闭失败零残留报告，见`P1_TASK_CUE_MOTION_QA_2026-08-07.md`。
- 全量回归捕获并修复AI连续服务的Windows停机竞态：关闭线程唤醒阻塞任务管道后，主循环此前未确认内部唤醒消息，导致发送端等待客户端超时并拖慢线程回收；现先确认再退出，提交后ACK与失败不ACK语义不变。定向真实执行20/20通过，见`P1_AI_SERVICE_SHUTDOWN_QA_2026-08-07.md`。

## 最近验证

- `npm.cmd run check`：通过，25个文件/147项前端测试，覆盖一键暂停、三类事务系统卡白名单、九类动作无文字、基础陪伴事务过滤/正式选择流程、“理一理”数据去向与单次重新授权、任务守望入口/刷新/错误隔离/隐私边界/暂缓与恢复、任务有限动作与减少动态、猫咪道具点击、专注结束与重逢固定无障碍语义、三档道具标签、诊断原生选择器取消/保存语义和Windows强制颜色样式门；
- `npm.cmd run verify`：通过，25个文件/147项前端测试、4项PID复用执行器测试、4项PID复用证据验证器测试、4项发布二进制边界规则测试、8项Claude v2证据测试、诊断证据7项篡改回归、证据包结构和35动画/3图集；
- `cargo test --workspace`：通过，共527项Rust测试，其中524项通过，另有3项需要符号链接创建特权或完整交互式Windows登录凭据的发布门按设计忽略；新增3项诊断导出回归覆盖规范敏感扫描、无覆盖原子私有写入和非法/远程/缺失父目录关闭失败。“理一理”现含10项授权、10项Provider/四格输出、9项独立IPC Schema与敏感清零、10项可信宿主/监听、3项源码隐私边界、4项AI崩溃隐私策略、3项私密引导、23项Windows管道单元回归、1项同用户独立进程拒绝、5项AI签名启动门及5项真实AI子进程安全/隐私回归；新增2项管道回归固定预读策略在身份核验后、正文分配与读取前执行。稳定核心监督器8项测试覆盖精确正文捕获日志零命中，未注册面板命令门3项覆盖Schema、窗口来源及内生请求标识/session。此前与前端验证并发执行的默认回归曾在主程序测试进程末尾出现一次`STATUS_ACCESS_VIOLATION`，后续完整复核未复现，不把首次异常从证据中省略；
- 主程序测试进程累计出现三次间歇性Windows `STATUS_ACCESS_VIOLATION`；最新一次发生在安装失败恢复改动后的全量`cargo test`末尾，崩溃前输出均为通过，随后主程序库单线程191/191通过（1项按设计忽略），再次执行全量`cargo test`通过。此前默认并发与单线程库测试均通过，完整180项库测试连续20轮共3,600项通过，真实Codex信任定向检查40/40通过。代码审计已修复一处独立成立的证书链指针读取隐患，修复后8线程并发真实适配测试连续20轮共160次通过；因没有崩溃转储，仍不宣称根因已证明，风险保持开放，见`P0_WINDOWS_TEST_STABILITY_QA_2026-08-07.md`；
- `cargo test --workspace --all-features`：通过，共541项Rust测试，另有3项按设计忽略；4项非默认PID复用执行器、4项AI崩溃隐私和2项预读闸门回归通过，运行、迁移和保留负载验收继续覆盖各自隔离、CLI、安全报告与核心不变量；`cargo clippy --workspace --all-targets --all-features -- -D warnings`通过；
- `cargo fmt --all -- --check`：通过；
- `cargo clippy --all-targets --features runtime-qa,migration-qa,retention-qa -- -D warnings`：通过；
- `cargo build --release --workspace`：通过；
- `npm.cmd run tauri build`：再次通过，v1.4.0 NSIS安装包生成成功；
- `npm.cmd run release:boundary`：通过；生产目录8个文件中无开发实验台标识，Tauri未声明实验辅助进程；
- `npm.cmd run release:binary-boundary`：通过；默认正式主程序不含运行、数据库迁移、任务保留负载验收、Support Sort精确正文金丝雀及PID复用执行器使用的17个专用标记，AI release原型也不含正文金丝雀测试开关、强退注入开关、测试Provider身份、固定正文金丝雀和动态崩溃金丝雀前缀5项标记；同时要求AI成品存在`WerSetFlags`、`WerGetFlags`与LocalDumps路径3项崩溃隐私正向标记。旧AI成品重建前按设计失败，重建后通过；该检查已接入预检和严格发布门；
- `npm.cmd run release:manifest`：在令牌、ProfileList与Known Folder一致且无产品注册的隔离标准账户通过；生成`src-tauri/target/release/release-manifest.json`，签名状态保持`not_performed`，未触碰Administrator现有0.1.2安装；
- `npm.cmd run ai-off:verify`：通过；4项边界逻辑测试、1项真实Rust离线核心测试和11项定向前端降级测试通过，扫描28个前端文件与31个稳定核心Rust文件；
- `npm.cmd run database:migration:test`：通过；9项非默认迁移验收测试覆盖合成 v6 完整链、非 v6 拒绝、既有报告不可覆盖、关闭态 WAL 稳定快照与单文件规范化，以及迁移/捕获两个CLI的完整合同和显式 v1.3.2 来源声明；该命令已纳入`verify`，但不替代真实 v1.3.2 副本；
- `npm.cmd run database:retention:test`：通过；3项负载核心与2项CLI测试覆盖固定点、路径脱敏、错误声明、相对/既有报告拒绝、百分位和年龄分布；该门已纳入`verify`；
- `npm.cmd run release:preflight:test`：通过，17项发布门逻辑测试覆盖许可证安装边界、NSIS实际安装载荷/清理、隔离与默认安装路径转换、损坏候选/主程序替换受阻恢复、默认保留/显式删除卸载数据选择、许可证、SmartScreen/第三方安全软件、RFC3161、无障碍人工矩阵及完整升级回退签字未声明/虚假声明分型、状态文案、AI关闭证据、默认正式程序冷启动来源/Profile绑定、路径逃逸、未签名拒绝、同证书一致性和Defender证据的候选绑定；另有4项首次启动恢复证据测试覆盖规范化数据库正向样本、错误来源/架构/覆盖拒绝、报告篡改和样本篡改；
- `npm.cmd run release:signing-protocol:test`：通过，3项合同测试覆盖三项最终产物与实时Authenticode事实、同证书/精确发布者/时间戳、签名工具、执行证据和RFC3161 SHA-256参数的正向链，并拒绝AI操作人、SHA-1、旧式时间戳、凭据暴露、证书或时间戳漂移、候选包漂移及策略未冻结时的伪通过；当前只生成2,833 bytes确定性协议包和空模板，实际签字文件不存在，独立验证器按设计退出2；
- `npm.cmd run release:sbom`与`npm.cmd run release:licenses:verify`：通过，生成507组件CycloneDX、0缺失声明清单及301个第三方生产组件的确定性许可证全文归档；
- 隔离账户刷新全部候选绑定子门后，`node scripts/generate_release_preflight.mjs`诚实报告13项自动证据通过、14项待完成、0项失败、`readyForRelease=false`；NSIS实际安装载荷、隔离与默认路径安装器文件转换、损坏候选/主程序替换受阻、部分主程序写入后Job终止与原始候选恢复、合成全新数据库首次启动写库终止与同候选恢复、默认保留/显式删除卸载数据选择和默认正式程序新配置冷启动门均已取得当前候选正向证据，`default_install_control_panel_registration`与`accessibility_acceptance`保持待办；严格门仍因控制面板注册、签名、安全软件、真实掉电/系统重启、真实历史数据库迁移和人工门按设计阻断当前候选；
- 首轮主程序替换受阻探测发现旧NSIS会返回0，却保留v1.3.2主程序并覆盖卸载器、写入v1.4.0许可文件。现有安装前置钩子会在任何复制/注册/快捷方式写入前独占打开旧主程序；占用时精确返回32。修复后损坏候选返回2、替换受阻返回32，两种情况下官方旧核心/卸载器、许可缺席状态与合成数据哨兵均不变，随后原始候选恢复安装和卸载通过，见`P0_RELEASE_INSTALL_FAILURE_RECOVERY_QA_2026-08-09.md`；
- 真实卸载界面探测证明数据复选框初始关闭；默认卸载保留LocalAppData/RoamingAppData合成哨兵，明确勾选后两处才被删除，且卸载/完成按钮、进程归零和最终清理均进入严格报告，见`P0_RELEASE_UNINSTALL_DATA_CHOICE_QA_2026-08-09.md`；
- 首次启动恢复探针在合成空数据目录观察到4,096 bytes主库与32 bytes非空WAL后终止1% CPU受限Job；终止后WAL为160,712 bytes，再以同一NSIS安装核心恢复到可见窗口、schema 11、`quick_check=ok`、1行默认设置与2行默认提醒。独立捕获器保留118,784 bytes、无WAL/SHM/journal旁文件的规范化SQLite样本，校验器重开样本并复算全部候选/脚本/报告/数据库哈希；正式数据、临时目录和进程最终均为0，见`P0_RELEASE_FIRST_START_RECOVERY_QA_2026-08-10.md`；
- 最新Tauri构建目录/便携主程序为22,192,640 bytes，SHA-256为`1B88183CFC5C225E7617EB50EFD6CD0DA1FC90A047A9135233AACA7354D0DC7B`；NSIS实际安装主程序同为22,192,640 bytes，SHA-256为`143B59A18FCE473858B00725DA6ADE427FA16E91CECDC402DC00CF300416121D`；
- 最新NSIS安装包为12,500,456 bytes，SHA-256为`C96D883452FC02B296AF13E92633C2282E261C5B3211BC949987DE39A3A66F96`。构建产物位于已忽略的`src-tauri/target`，未新增Git跟踪项；NSIS载荷报告SHA-256为`131ABBEEA6A54A1B89E3129CEF154007ED5765F76860855436418AAC679D4BAD`，隔离安装器转换报告SHA-256为`5ECDB9CC0F26C59655D6A06E9A84CF0CA95D5FE2A091B5062F6487A34109B778`，默认安装路径转换报告SHA-256为`7C986BDD270A670E810C535472C14D1A2A79FD4C2756F43515BA6B3F3FD07496`，安装失败恢复报告SHA-256为`7C0141D0522C6012F110DB70B96A556AABE61848D4DE2394A3E4E7488F3FFCBA`，首次启动恢复报告/数据库捕获报告/规范化样本SHA-256分别为`DB571D29F22648EA6196E4A0364A7242DFB7C3894DDFB17A4203046A716B6AEA`、`CD506F72A6AC2AD0F8818D2B24066019EACD0BE08A0412FAB41C969CF7C6022A`与`2F78DD5C8E81551194DEC0618D8D9004BA1727082B12143678125F807D0A8832`，许可证人工复核包为8,938 bytes、SHA-256 `C25E553E76012FE5608916A2D40278AF957D9B55ACB5E57DEADDF941AAD9F32A`，外部信任测试包为2,459 bytes、SHA-256 `E8279E9715D00E04DAEC2F5E1F6FF8524A6E501FA2EBAC5119EC786BC251F195`，签名协议包为2,833 bytes、SHA-256 `2A6E1F71A8A6FED998661F25F27114B6350E26210A7F6FC98B1A028B7B9E11C6`，无障碍人工验收包为3,762 bytes、SHA-256 `F7D34C54BAE26C787A900ADB1F7E33904E453C2CDFCA5BAAAC56EB9A3A946FDE`，完整升级回退包为5,064 bytes、SHA-256 `501C93980CCBEDE02864E48B5C03A1722D05E8DEAE064CF52A4A54C31378315B`，卸载数据选择报告SHA-256为`9293C91BEF03517332B79AB978B651A0F7C1D6D88AE1F6978747B052C6E971F0`，发布清单SHA-256为`07B77F05FCC68AE4BDD34C4EBD26330D65E2D4C6E6F283BD533FF6796BFA9DCC`，冷启动报告SHA-256为`288B42C4F285295B25884A053F28B1A6E419EEC1741389180D86C55ABB94D9C1`，AI关闭证据SHA-256为`6A1166A23568574BECEE4164A71E36D7EF2A96D4336BCE15FCC4A6BD19DA3DE5`，发布预检报告为7,540 bytes、SHA-256 `C733DF6567E0C47070A92F55440FF00BC1545A4D4001BB74BABA3D1E29C45434`；
- 两种正式主程序变体的字节检查均不含17个非生产验收/Support Sort正文金丝雀/PID复用执行器环境变量、配置值、标记文件名、夹具名、数据库迁移或任务保留负载验收标记；NSIS安装变体已在独立Windows测试账户完成3轮新配置真实可见窗口启动，P50/P95为136.4/230.1 ms，AI子进程和Application Error均为0，三轮数据根及临时发布根全部删除。隔离任务、数据库迁移、保留负载和PID复用夹具均未进入主程序或NSIS声明；
- 工作区release构建会生成独立`yuanyuan-bridge.exe`原型，但Tauri配置没有`externalBin`或资源收录项，因此当前NSIS安装包不包含Bridge。
- 独立`yuanyuan-bridge.exe` release原型为2,511,360 bytes，SHA-256为`6E9B9D56A40A6AED7AA89ED4E7BBBEFBF45E6BF25A9DA6914BF55E931ACEB84D`；
- 独立`yuanyuan-ai.exe` release原型为2,845,184 bytes，SHA-256为`7EBE0E6C346A3159E68B3D3F8DB6E7217FEF5F8E9270AFD619BFDBD29C4BD1A6`；AI在私密引导和本地存储前应用WER无堆并对全局/本进程LocalDumps配置失败关闭，正式二进制不含强退注入开关或动态金丝雀前缀，当前NSIS安装包同样不包含AI进程；
- 便携主程序、NSIS实际安装主程序、Bridge/AI两个原型和当前NSIS安装包均未签名，P0-E08发布门保持关闭。
- P0-E01新增隔离性能采样器：`baseline-ai-off`只显示宠物、隐藏面板，提醒库/AI库/WebView数据与正式用户隔离，30秒至72小时到时走应用自身退出路径；脚本以10秒预热和逐PID相邻增量统计Tauri/WebView2整棵进程树，并同时检查数据库增长、AI子进程、Application Error、正式目录写入和安全清理；
- P0-E01提醒端到端延迟冻结门已在可交互Windows桌面完成20个真实窗口样本，2/5/8/11/14秒五个调度相位各重复4次且20/20通过；后端领取P50/P95为6,903.5/12,938.9 ms，领取到窗口交接P50/P95为240.9/281.0 ms，总呈现P50/P95为7,150.2/13,167.8 ms，均低于冻结的16,000/1,000/17,000 ms上限。采样器使用生产前端资源、独占运行锁、每样本唯一标题、完整强提醒无障碍名称与`scheduled <= claimed <= presented`硬校验，报告schema v2绑定主程序、夹具和脚本哈希；应用均自身退出且隔离目录零残留。独立校验器会重算逐样本延迟和P50/P95并重新绑定三份来源，5项回归覆盖BOM、来源过期、相位/字段漂移和乐观篡改；当前报告独立复核退出0。1样本负向门证明短测不能冒充基线。该结果只关闭暖系统隔离验收版门，不替代正式默认二进制、24小时、睡眠或签名候选，见`P0_REMINDER_LATENCY_QA_2026-08-06.md`；
- P0-E01默认正式程序新配置冷启动门已在独立Windows测试账户完成3次正向采样：从当前NSIS精确提取的实际安装主程序按清单哈希复制到不含Bridge/AI原型的随机所有目录，三次真实可见窗口为230.1/136.4/130.2 ms，P50/P95为136.4/230.1 ms；AI子进程和Application Error均为0，三轮数据根、临时发布根和圆圆进程零残留。普通账户下的WMI权限失败曾暴露进程未清理缺陷，现改用Toolhelp原生快照并对WebView锁释放做有界重试；schema v2报告绑定采集脚本，并以当前SID的Windows ProfileList证明测试目录属于实际令牌，环境变量不能伪造账户。发布清单在产物不变时保持稳定哈希，预检已重新核对并接受该证据，见`P0_RELEASE_COLD_START_QA_2026-08-06.md`；
- P0-005“AI全部关闭”组合门已接入每轮`verify`、发布预检和严格门：生产CSP只允许Tauri本地IPC，28个前端文件和31个稳定核心Rust文件无外部网络入口，Tauri安装声明不含Bridge/AI；真实Repository测试证明AI不存在时到期提醒、专注、完成和今日列表继续工作，前端无组件时隐藏实验卡。候选报告绑定release清单、便携主程序、NSIS实际安装主程序和NSIS安装包哈希，见`P0_AI_DISABLED_REGRESSION_2026-08-06.md`；
- P0-004 已具备严格的 v1.3.2 数据库验收入口：只接受操作者显式提供并声明来源的关闭态 schema v6 普通文件，原样本只读且前后 SHA-256 必须一致；随机副本通过生产仓储迁移到 v11，以旧表原列规范化多重集合证明每条旧记录逐值保留，再执行生产备份恢复和强制迁移失败后的安全快照回滚。新增关闭态 WAL 源库捕获入口，以主库/WAL 前后哈希、SQLite Backup、无 sidecar 规范化和逻辑逐值摘要证明稳定副本；9项功能/CLI回归与默认回滚回归通过。GitHub 正式 v1.3.2 Portable 的版本、`D142095E…CFF89AD`资产摘要和发布校验文件均匹配，但在当前独立普通账户的5种启动条件下只创建`EBWebView`且未进入`setup`，其中最新一次已把子进程环境显式校正为当前令牌经`ProfileList`注册的真实配置目录；仍没有生成旧库，所有临时进程/目录已清理，现有用户数据库内容未打开、复制或修改，正式用户目录零写入。因此真实样本门保持未完成，见`P0_V132_DATABASE_MIGRATION_QA_2026-08-06.md`；
- 60秒真实Tauri烟测通过：暖系统启动到可见宠物205.5 ms，24逻辑处理器归一化CPU平均0.0684%、P95 0.2117%，平均工作集409,912,186 bytes、平均私有内存202,098,421 bytes，数据库相关文件增长4,120 bytes，正式目录写入0、AI子进程0、Application Error 0、受控退出码0且隔离根已删除。该结果不外推为24小时趋势，见`P0_RUNTIME_BASELINE_QA_2026-08-06.md`；
- P0-003已补齐24小时常驻、睡眠/唤醒和锁屏/解锁的严格执行门：固定请求86,400秒、60秒采样和300秒预热，按相邻样本最多计两周期要求至少20小时活跃覆盖；真实Windows `suspend→resume` 与 `lock→unlock` 事件缺一即失败。工作集、私有内存、句柄和线程按6段中位点重算斜率，并与首末段增长、CPU和数据库增长共同受冻结双门限制。schema v2报告绑定主程序、夹具和执行脚本；独立复核器拒绝来源过期、样本/趋势篡改、未知字段、事件缺失、乐观通过和短测冒充。30秒真实窗口执行链短测通过且被严格复核器按设计拒绝；真实24小时正向运行尚未开始，见`P0_RUNTIME_ENDURANCE_GATE_2026-08-09.md`；
- P1-E10三类任务提示已在当前150% DPI真实Tauri窗口完成完整/减少动态验收：失败、等待用户、疑似停滞的完整动态不同帧分别为16/21/16，减少动态均为1；六个样本均确认固定无障碍语义、预启动中性隐私背景、应用/背景受控退出和隔离根清理。人工复核确认失败察觉、抬爪等待和检查收拢动作语义正确且固定道具持续；schema v1视觉清单以SHA-256绑定候选、夹具、三份脚本、三份报告、六张联系表和六个GIF，独立校验器及4项篡改回归通过。无效探索产物24项已删除，含一组曾捕获桌面的隐私敏感截图。该证据仍不替代多DPI、高对比度、Narrator、真实签名Hook、睡眠/锁屏或签名候选，见`P1_TASK_CUE_MOTION_QA_2026-08-07.md`；
- 非语言表达、三路径基础陪伴及断开/恢复三个开发实验台此前已完成宽屏与390×620浏览器实拍检查；正式三路径入口、三档标签设置也已取得当前150%环境的真实Tauri证据。任务守望进一步用隔离的合成认证事件取得真实猫咪道具、状态卡、Windows UI Automation控件树、页面Tab顺序和点击唤起面板证据；新增暂缓/恢复在390×620开发页面完成真实点击、固定无障碍名称、状态持续可见和卡片无内部横向溢出检查。正式用户目录零新增写入且运行后清理完成。该证据不等于真实签名Hook，也不替代正式Tauri暂缓交互、完整多档DPI、高对比度、文本缩放、Narrator人工体验、全应用键盘和目标用户辨识。
- P0-065新增“理一理”数据去向与重新授权开发原型：正文输入框出现前必须选择本机/云端、查看固定数据流并明确确认，前端回执绑定用途、内容类别、Provider身份/指纹、披露版本、五分钟有效期和单次使用；切换去向、Provider变化、过期、使用或取消均要求重新授权。状态机与页面不含正文、网络、存储、日志或后端调用，1280×720与390×620真实点击无横向溢出，生产构建排除实验台。`yuanyuan-ai`同步新增内存单次授权核心、授权后固定请求层及专用结果门：可信Rust描述与适配器成对绑定，授权消费后只构造固定Schema和本次用户原文；令牌、身份及额外上下文无外发入口，通用模型回复/动作不会越过API，工具层只收到“事实、感受、可控、下一步”四字段。前端到Rust命令、真实Provider请求、语义专业复核与崩溃零残留仍未实现，见`P0_SUPPORT_SORT_DATA_BOUNDARY_QA_2026-08-07.md`；
- P0-065补充独立本地请求协议v1 Schema、AI进程内可信分发器和Windows隔离传输原语：不扩写健康/关闭ACK控制通道，也不复用任务事件管道；`describe_provider → issue_authorization → submit/cancel/revoke_all`严格分型，授权前没有正文入口，提交只允许一次性授权绑定和本次原文，完成响应只允许四格。9项协议与10项可信宿主/监听回归证明披露摘要由可信注册表生成、session不匹配关闭失败、配置替换全撤权，适配器只在正文校验和授权消费后创建；敏感协议类型不提供`Debug`/`Clone`，session、令牌、正文、四格结果及Support Sort专用JSON/管道缓冲在正常释放时主动清零。独立管道在读取前后分别核对客户端PID，以打开的进程句柄核对创建时间并拒绝已有退出时间的身份，同时对双向载荷设限；连接、写入和响应共用3秒硬截止；32字节随机session只经继承stdin送入AI进程，错误引导在数据库启动前拒绝。23项管道单元回归、1项同用户独立进程回归、3项引导单元回归和5项真实AI子进程回归进一步证明静默服务端按时返回、伪造身份、已退出进程句柄及同账户非授权进程关闭失败、直接父进程身份不符时数据库启动前拒绝、连续实例重建不丢消息、策略闸门在身份核验后且正文读取前拒绝、强退后必须用全新session重启，以及正文金丝雀不进入AI标准流或隔离本地文件。监听器以非消费式管道探测把调用者断连和AI停机接到Provider取消令牌；真实慢Provider回归连续验证占用期间第二请求按自身预算超时、调用者断开后工作收到取消、服务重绑可用，停机也触发取消。稳定核心现已接入关闭态的AI签名启动门，精确正文闭环的捕获日志为零；未注册面板门只允许`panel`窗口且由核心内生请求标识/session。PID复用执行器在第424次创建取得真实同PID不同创建时间样本，独立验证器证明旧身份在载荷读取前被拒绝；强退/转储/换页两阶段工具已实现但尚无隔离主机实测证据。正式Tauri注册后窗口金丝雀、真实签名/包身份正向认证、第二账户和真实Provider仍未完成，见`protocol/SUPPORT_SORT_IPC_PROTOCOL_V1.md`；
- P0-065新增正文源码隐私门和普通释放清理：Provider请求递归清理JSON字符串，原始模型输出字节、授权能力/调用、四格结果及面板命令敏感字段主动清零；过期淘汰、消费失败、取消、全撤销和授权门释放都会清理令牌与Provider身份。敏感内部类型不再自动派生完整`Debug`，必要格式固定为`[REDACTED]`；3项源码回归禁止十一段生产链加入日志/控制台宏、恢复敏感自动`Debug`或移除显式清理，新增两段为AI崩溃隐私策略和命名管道预读闸门。真实AI调试子进程用唯一正文金丝雀完成披露、授权、Provider和四格闭环，关闭后标准输出/错误输出为空，隔离SQLite、锁文件及全部本地文件递归扫描零命中，也未创建日志、诊断或备份目录；稳定核心类型化闭环捕获日志零命中，release主程序和AI同时证明精确正文/测试入口标记不存在。该证据不替代正式Tauri注册后窗口路径、真实Provider、强退转储、换页和网络取证，见`P0_SUPPORT_SORT_PRIVACY_GATE_2026-08-07.md`；
- P0-065新增AI敏感进程崩溃隐私闸门：非关闭辅助路径在私密stdin引导、`LOCALAPPDATA`和数据库前先应用进程级WER `NOHEAP`，再只读检查64位HKLM中的全局/`yuanyuan-ai.exe`专属LocalDumps配置；策略不可用、注册表无法可靠读取或发现配置时直接关闭失败。每次已认证Support Sort连接又会在身份核验后、正文分配和读取前重新复核。本机观察为四个受识别全局值0项、AI专属键不存在且未修改注册表；4项策略回归、2项预读闸门回归、十一段源码零日志门和release AI中的3项正向标记均通过。该闸门不覆盖检查后的竞态、管理员调试器、外部转储工具、页面文件/休眠文件或尚未执行的真实强退取证，见`P0_AI_CRASH_PRIVACY_GATE_2026-08-07.md`；
- P0-065新增两阶段强退取证执行器：独立target中的QA注入版release AI只有在真实完成Provider描述、单次授权并收到运行时动态正文金丝雀后才异常终止，捕获器扫描隔离应用目录、标准流和至少一个专用转储根；离线终结器拒绝在线页面文件替代，要求至少一个停机后取得的页面文件副本，并要求休眠/交换文件明确关闭、缺失或提供副本。最终验证会重新绑定三份源码、捕获报告和全部离线文件SHA-256并复扫UTF-8/UTF-16LE金丝雀。9项AI参数规则、4项Rust捕获规则、4项Node终结/跨块扫描规则、QA release独立构建及Clippy均通过；正式AI新增2项QA禁用标记且二进制边界通过。当前没有隔离主机执行报告，故取证门仍待完成，见`P0_CRASH_PRIVACY_FORENSIC_QA_2026-08-08.md`；
- P0-065稳定核心监督器现已把私密引导接入真实AI启动epoch：每次启动/重启生成新session并通过继承stdin写入，写入失败立即杀掉子进程；启动失败、AI退出、熔断、关闭和显式重启都会移除父进程持有的session，且只有Running状态可视为内部会话可用。AI在控制健康门开放前绑定私密Support Sort管道，以单实例、单请求串行服务形成零队列背压；无Provider配置时只返回固定“不可用”，未知字段或中途截断的JSON消息零响应丢弃后可重绑继续服务，停机通过取消阻塞管道I/O并等待线程清场。稳定核心新增不暴露session/管道的类型化内部请求端，真实AI子进程已完成一次查询往返；未注册面板门进一步只允许`panel`窗口，外部不能提供请求标识、session或额外上下文，请求标识和session均由稳定核心控制。该模块刻意没有Tauri命令标记且未加入`generate_handler!`，正式安装声明继续排除AI原型；
- P0-065非默认PID复用压力门已取得真实正向证据：共享命名管道源码变化先令旧256次报告按哈希失效；刷新256次仍无复用后，4,096次有界请求在第424次真实出现同PID不同创建时间，新进程使用旧身份连接时在读取载荷前精确返回`ClientIdentity`。报告不含原始PID，记录完成424次、423个不同PID、`ready=true`，执行器和独立验证器均退出0；报告SHA-256为`7C552229BFD4E051E12C8D2C65661966C76F61FD6A891888478F6FD310732E62`，绑定源码SHA-256为`823607A29F3230DBCEB9B72CC62E552F1D30BA6388D49107807F093042F8434E`，见`P0_PID_REUSE_STRESS_QA_2026-08-07.md`；
- 敏感缓冲清零使原有“服务端确认后销毁旧实例、再创建下一实例”的极短窗口稳定暴露：紧邻的第二条任务事件可能在`FILE_NOT_FOUND`时直接放弃。命名管道客户端现会在原有连接预算内等待实例重新出现，缺失AI仍按预算返回Unavailable；新回归以25毫秒重建间隙验证连续两条消息均获确认，AI连续服务测试恢复通过；
- 实例重建等待进一步暴露了监督器的分层时限问题：20毫秒启动门曾调用默认1秒健康探测，导致外层截止失真。健康与关闭控制请求现使用有界双向传输，健康探测只取得本轮剩余预算且单次最多50毫秒；异常子进程熔断与显式重试的真实进程回归重新通过；

## 下一批实现顺序

1. 私密session内部请求端、AI独立监听、调用者断连取消、直接父进程链、PID查询前后快照/已退出句柄拒绝、单实例并发背压、原生客户端截断写/放弃读恢复、关闭态签名启动门及真实PID复用正向报告已经完成；继续取得真实同证书签名/包身份正向证据与第二账户矩阵后，才允许注册Tauri入口；
2. 在具备第二Windows测试账户或令牌的环境完成真实跨账户进程拒绝，并使用历史签名AI验证新核心、历史核心验证新AI及升级中断矩阵；
3. 由用户确认发布主体所在地区、个人/组织身份与预算，据此在Artifact Signing Public Trust、受信CA证书和Store MSIX Spike间冻结渠道与精确发布者Subject；随后按已冻结的外部信任测试包，在启用Defender的干净机、SmartScreen和两款第三方安全产品完成候选绑定预演与具名人工签字；
4. 保持项目级入口严格只读，补齐双方签名/包族/密钥轮换、Claude历史/后续版本与arm64实体文件矩阵、具备特权的祖先重解析点矩阵及真实Tauri窗口/键盘验收；Bridge签名与安装升级门完成后，才评估真实Hook授权入口；
5. 完成真实系统睡眠恢复和长期连接/数据库增长验证，校准已接入的7/90/180/365天清理器、6小时周期及每轮256页增量回收；既有库默认继续不转换；
6. 诊断卡片已关闭当前150% DPI下的真实Tauri窄窗、键盘焦点与自动无障碍树验收；继续在100%、125%、200% DPI完成同一证据链，并执行高对比度、文本缩放与Narrator人工听读；
7. 本机另存为与独立敏感扫描正向链已经完成；继续执行第二账户、满盘、目标占用和安全软件矩阵，并补齐其余DPI与辅助功能人工验收；NSIS卸载器可选清理交互已取得候选绑定证据；
7. 脱敏任务守望页面、道具点击入口及返回动作安全门禁已经以只读/无外部处理器方式先行；上述证据稳定后，才接入真实签名Hook任务链，并将Bridge/AI与逐来源返回处理器纳入安装、升级和回退矩阵；
8. 为安抚支路准备职业健康/心理健康顾问场景包和具名签字流程，并由目标地区复核人员建立第一份真实静态资源包；完成前只保留三路径基础陪伴集。

## 当前边界

P0尚未完成，v2.0A也未达到可用状态。协议、Bridge、匿名令牌拒绝、正式凭据生命周期与脱敏确认控制面、只读安装发现、授权/Hook/健康分层、连续认证失败暂停、官方Hook格式/Schema复核、多事件/多来源只读所有权预览、项目级原生选择器/密封目录能力/正式零写入确认入口、来源工具签名/发布者/分发位置与离线第二因子复核、Claude 2.1.211真实发布证据与x64实体双因子验证、无损接入与撤销、私有事务、官方目标封装、断开/撤权后维护状态机及开发交互原型、返回动作静态注册/一次性凭证/双重校验与内部安全降级、CLI成功路径、独立AI任务库、满盘/只读/写锁/独占占用零写入恢复、真实进程强制终止恢复、“提交后ACK”、单实例、优雅停机、v1健康协商、真实v1/v2调试子进程矩阵、熔断后的显式恢复、条件显示的本地诊断卡片及其150% DPI真实窗口/本机另存为/独立扫描验收、真实崩溃熔断、提醒核心隔离，以及安抚三路径/只听不记/偏好/资源包的工程边界已经连通；但真实Hook授权产品入口、真实来源返回处理器与正式按钮、双方签名/包族/密钥轮换流程、目标版本路径/载荷矩阵、历史及后续签名二进制与arm64实体矩阵、真实第二账户、系统睡眠恢复、其余DPI/Narrator窗口验收、长期增长证据、安抚专业签字和真实地区资源仍未完成。在这些门禁完成前，不应声明真实任务守望、返回原工具或完整Support Box已实现，也不应把辅助进程加入发布安装包；项目级入口仅代表用户主动触发的本地零写入检查。
