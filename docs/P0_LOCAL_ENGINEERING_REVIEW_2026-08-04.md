# P0 本地工程复核报告（2026-08-04）

结论：本地可完成的首轮架构、安全边界和故障 Spike 已形成可重复证据；允许继续 P0 内部开发，不批准配置真实 Hooks、不批准把 Bridge/AI 加入安装包、不批准宣称 v2.0A 或完整 Support Box 可用。

## 1. 决策

| 决策对象 | 结论 | 原因 |
| --- | --- | --- |
| 继续本地P0工程 | GO | 现有提醒核心、实验进程、协议和数据库边界可继续独立验证 |
| 进入真实连接器试点 | NO-GO | 缺真实脱敏载荷、配置生命周期、历史签名兼容和长期运行证据 |
| 将Bridge/AI加入NSIS | NO-GO | 当前未签名，连接器发现/Hook配置生命周期、升级/回退和干净机矩阵未完成 |
| 发布v2.0A | NO-GO | 24/72小时、睡眠恢复、第二账户、正式道具/盲测和签名门未完成 |
| 发布三路径基础陪伴 | 仅可继续原型 | 交互与工程边界通过，仍缺正式动画、真实Tauri/无障碍与用户研究 |
| 发布完整Support Box | NO-GO | 缺职业健康/心理健康签字和真实地区安全资源；完整入口保持关闭 |
| 启用云Provider | NO-GO | 只有回环契约，网络、SSRF、凭据和真实数据流预览未实现 |

## 2. 当前可重复证据

- 前端：14个测试文件、75项测试；35个动画和3张WebP图集校验通过；
- Rust：协议32项、Bridge96项、连接器12项、AI57项、稳定主程序52项，共249项测试；另有2项完整登录凭据与符号链接特权发布门本轮单独通过；
- 严格检查：AI、Bridge、Protocol 全目标 `-D warnings` 通过；全工作区仍有两处既有主程序 Clippy 告警，不在本轮借机重构；
- 构建：release workspace、Tauri主程序和NSIS安装包通过；
- 生产边界：`dist` 8个文件中没有表达/安抚开发实验台代码、文案或样式；Tauri `externalBin/resources` 未声明Bridge或AI；
- 发布清单：固定记录四项产物的角色、大小和SHA-256，签名状态明确为 `not_performed`；
- 视觉：非语言任务表达和三路径基础陪伴均完成宽屏与390×620浏览器检查，控制台无警告/错误；
- IPC/故障域：当前用户DACL、匿名拒绝、HMAC、重放、提交后ACK、暂存、真实进程崩溃/熔断和提醒核心隔离通过；
- 数据：AI库v1→v2→v3→v4迁移、失败回滚、精选记忆/偏好墓碑防旧备份复活及任务证据安全投影通过；
- 安抚：双质量门硬裁剪、只听不记无持久化/网络/模型API、类型化偏好、资源缺失不猜号码通过；
- 诊断：无自由正文Schema、64KiB/5文件、原子写入、当前用户DACL和匿名读取拒绝通过；真实AI监督状态、只读队列规模和十类固定错误码尽力计数已接入“预览→选择保存位置”。用户导出只接受本机普通驱动器、无覆盖写入并在临时/最终复读时独立扫描，不回传绝对路径、不创建应用内副本或自动上传；精确清理会保留用户另存文件、队列、提醒、备份与无关文件；
- 增长：任务事件7/90/180/365天策略、最新事件豁免、批次和故障整体回滚通过；常驻AI在健康就绪后延迟5秒异步清理一次，启动健康与快速退出不受阻塞。

最新产物细节以忽略目录中的 `src-tauri/target/release/release-manifest.json` 为准；当前清单生成时间为一次本地构建证据，不是可信签名时间戳。

## 3. P0 Epic复核

| Epic | 本地工程状态 | 仍缺硬门 |
| --- | --- | --- |
| E01 v1.4基线 | 功能/资源/构建回归稳定 | 真实数据副本、性能、24小时、多DPI、干净机 |
| E02 架构与安全 | ADR、威胁模型、协议和故障域已形成 | WS0/1/2/8具名负责人和风险接受人 |
| E03 IPC/进程 | 高价值本地矩阵已连通 | 历史签名二进制、第二账户、睡眠、长期增长 |
| E04 连接器 | 合成契约和本机静态词汇证据 | 官方真实脱敏事件、支持版本冻结、配置撤销 |
| E05 非语言 | 导演、标签三档、开发实验台 | 正式道具动画、新用户盲测、辨识率冻结 |
| E06 AI/记忆 | Provider契约、中文FTS、记忆/备份边界 | 真实Provider、扩大金标、加密备份实现和安全复核 |
| E07 安抚 | 工程裁剪、隐私、偏好、资源包和原型 | 顾问签字、真实地区包、用户研究、真实崩溃敏感扫描 |
| E08 发布 | 生产排除扫描、哈希清单格式 | 证书/服务采购、签名、RFC3161、SBOM、误报和回退预演 |

## 4. 下一阶段顺序

1. 指定WS0/WS1/WS2/WS8负责人、接手者和风险接受人；
2. 在第二Windows账户、睡眠恢复和24小时常驻环境补齐IPC/增长证据；
3. 本机选择保存位置与独立敏感扫描已完成当前150% DPI真实正向验收；继续完成第二账户、满盘、目标占用、安全软件及100%/125%/200% DPI与Narrator矩阵，并验证常驻期间低频清理策略；NSIS卸载器默认保留/显式删除数据的候选绑定交互已通过，仍需完成卸载界面的辅助功能人工矩阵；继续保持无遥测、无自动上传；
4. 在已完成的凭据创建、两阶段轮换、立即吊销和单次确认控制面上，继续建立实例发现、Hook差异/备份/信任审核和仅撤销圆圆片段的正式用户流程；
5. 获取Codex/Claude官方真实脱敏载荷并冻结能力矩阵，之后才做受控连接器试点；
6. 并行启动安抚顾问场景复核和首个目标地区资源包运营流程；
7. 确定签名渠道与预算，完成四产物统一身份、时间戳、SBOM、误报和升级回退矩阵；
8. 只有各自质量门通过，才分别批准v2.0A、v2.0B Core和Support Box；一条支路延期不应绕过其门，也不应拖住已独立过门的支路。

## 5. 当前保护声明

工作区原有未提交修改全部保留，未执行 `reset`、`clean`、`checkout` 恢复或提交操作。当前安装包仍不含实验辅助进程，开发实验台也不进入生产资源。以上复核只覆盖本机可重复证据，不能代替具名负责人签字、专业复核、真实用户研究、代码签名或发布批准。
