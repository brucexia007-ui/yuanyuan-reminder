# Changelog

## 1.5.35 - 2026-09-22（源码更新，尚未发布安装包）

- 学习黑板增加结果显示空间，答题后保留所选项与正确项，结论及下一题无需滚动；学习宠物位于黑板下方，不遮挡内容。
- 学习待机保持静止，未作答一分钟后每题最多一次好奇动作；正确与错误的动作反馈继续播放。
- 本地完整 verify、Rust 测试和生产构建通过；本机已备份后更新，用户反馈人工核查正常。
- 用户取消本轮新的 24 小时观察。历史失败与未验项目保留，不宣称完成观察或稳定发布。

## 1.5.34 - 2026-09-21（源码更新，尚未发布安装包）

- 恢复逗猫棒原有八方向伸爪、挥爪与收回动作，修复改用起身动作后与旧版形象不一致的问题。
- 松手、取消指针和切换互动时清理动作循环；关闭动画时保持静态方向姿势。

## 1.5.28 - 2026-09-13（统一主程序与饺饺宠物包候选，未发布）

- 接入通用知识包的预览、进度、取消、选择题与回忆题、更新及完整原生导出；继续接受 CSV 和旧原生 JSON。学习库独立升级至第 8 版，保留旧卡片及复习进度。
- 重复导入保留排程；答案变化或排程版本增加时提前显示重置数量，拒绝排程版本倒退，提交前再次核对原文件。
- 逗猫棒按距离和时间推进，提醒操作反馈跟随当前昵称；保留原有互动清理、睡眠恢复和提醒优先级。
- 圆圆形象、服装和热身素材保持原样；工作专注静止同时覆盖没有情境图集的宠物。首次换宠物预加载所声明的学习与情境图集。
- 定制默认交付可恢复、可校验的独立宠物包，应用身份保持 `com.yuanyuan.reminder`。饺饺来源固定为 `d0317cf5e9d8324aaee0461d84b4589c4a2fdcc3`，原图、许可和 39 个动作节奏保留。
- 本版本是本地候选。原生播放、Windows 系统缩放及两版安装器升级/配套回退验收未完成；不得据自动测试通过视为正式采用或发布。

## 1.5.27 - 2026-09-12（工作专注静止，未发布）

- 工作专注直接保持该形象设定的静态坐姿，始终播放模式下也不再循环动作，减少工作时的视觉干扰；离开工作专注后，恢复精神、提醒及互动仍正常播放。
- 沿用现有形象与服装，不修改图集、动作名称、帧时长或业务状态机。

## 1.5.26 - 2026-09-11（圆圆形象修复候选，未发布）

- 以已确认的07形象修复热身提醒、热身循环、工作恢复、学习好奇及独立入睡/醒来行；热身保留长体态和扒屏支撑动作。
- 工作恢复采用已确认的象牙白背心、浅蓝亚麻衬衫、驼色细边与金框眼镜，另外三行工作动作仅统一服装。保留11行情境动作和熟睡循环的解码像素，基础、生活、独立学习、静态形象及动画清单文件不变。
- 内置圆圆热身按坐姿端点校准整行动画比例，窗口为提醒卡片预留独立空间，并提高原生窗口高度上限；取消该动作额外的整体跳动。其他宠物及提醒沿用原布局。
- 九行动画及衔接已通过原渲染组件100%/125%/150%播放与静态检查，12组体型/比例通过卡片间距和裁切检查；隔离原生窗口已观察站起、伸展、回位及活动完成后的窗口恢复。完整Windows系统缩放实机矩阵仍待验收，不自动发布或覆盖已安装程序。

## 开发中：情境图集验收工具修正（尚未替换运行时素材）

- 定位疲劳循环右侧断边来自装配时等宽切片，而非该原始动作行缺少身体；改为先提取完整八姿态，再统一缩放装配，拒绝源画布裁边、合并姿态及异常帧数。
- 添加 12 项工具回归、完整动作行增量替换，以及编码后重新解码核对 RGBA 和隐藏 RGB 的门禁。
- 继续为低伏/蜷睡循环加入相邻端点尺寸和基线注册，素材工具回归累计 22 项通过；两个完整循环行修复后，全 18 行独立静态复核通过，真实播放及系统缩放仍待验收。
- 候选未替换 `public` 素材；本段初次记录于 1.5.19 development，当前程序版本以下方修复候选为准，均未发布。不把单元或静态检查通过写成美术动态或安装版验收通过。

## 1.5.23 - 2026-09-09（学习结算恢复与原生导入遮挡修正候选，验收中，未发布）

- 1.5.22 实机发现第三题完成后被补水提醒遮挡，提醒完成后结算页消失、学习装扮残留；答题、复习及提醒记录没有丢失或重复。新增后端内存结算展示标记，在整个仲裁变更后按原优先级恢复；真正的答题暂停仍需手动继续，关闭结果或睡眠不会复活已结束会话。
- 前端仅保留后端明确指定会话的已完成结果，拒绝旧修订；新增 7 项组件回归，3 项首先在旧实现失败。后端覆盖七种高优先级遮挡、完成竞态、提醒队列、专注/任务、关闭及睡眠。
- 学习展开窗口会拦截重叠文件选择器的“打开”点击。宠物包、学习导入/导出和诊断保存对话框临时提升其所属面板层级，选择完成、取消或错误后恢复；不修改宠物置顶设置、业务状态或窗口保存坐标。层级状态覆盖嵌套与失败回归。
- 综合校验（前端 330 项及包工具 5 项）、Rust 工作区全量/无学习测试和两种 Tauri 生产构建通过；主库分别 339 / 243 项通过，各 1 项原有忽略。正常退出旧版后保留数据安装，239 个数据文件冷归档及安装前后哈希一致，实际主程序指纹匹配。schema 13、正式图集及沙盒业务数据不变。
- 新安装版能打开今日/新提醒页；键盘输入未转发，随后桌面工具激活及恢复返回访问拒绝，安全停止原生输入。两项缺陷仅源码回归通过，新安装版路径和原 E01–E30 仍未完成，不发布。
- 09:58 续验：用户确认真实 T 输入正常，鼠标学习导航可用，但开始学习时桌面通道再次访问拒绝，未计为产品失败或复测通过。同一进程仍响应并取得实际 150% DPI；新增 256 份快照仅出现此前已保存的 T 提醒，其余已采集业务字段不变。没有新增产品改动或版本递增，两项修正及完整矩阵仍待真实路径复测。
- 11:25 续验：原生导入遮挡 F32 的失败鼠标路径已在安装版通过，缺学习包 7 文件与原包相同，导入不自动切换且保留 Full/K。学习同一会话可在补水后继续，新增两答/两复习，无重复记录；980 份快照严格核对。换包时桌面激活再次失败，F31 结算恢复未执行到，完整矩阵仍未放行；未改产品或重新安装。

## 1.5.22 - 2026-09-08（快速拖动修正候选，验收中，未发布）

- 本地回归复现读取窗口起点/缩放期间忽略移动、提前松手后误打开面板的问题；手势意图现在同步记录，几何读取、移动确认与最终保存仍在同一顺序队列中。
- 松手使用最后坐标补足未收到的移动事件；连续快速手势分别使用上一手势确认的位置。取消丢弃未执行移动，已执行的移动只保存实际确认位置；卸载或切换形象使旧几何/保存回调失效，读取或移动失败不覆盖旧坐标。
- 新增 15 项组件回归，4 项首先在旧实现失败，修正后所在文件 41 项通过；完整前端 323 项与 5 项包工具测试、综合校验、Rust 工作区全量/无学习测试和两种生产构建全部通过，主库分别 333 / 241 项通过、各 1 项原有忽略。模拟缩放不等同 Windows DPI 实测。
- 首次启动脚本的空磁盘余量误判与原生 stderr 处理异常均保留记录；改正执行方式后完整重跑。只清理 8 个旧测试调试符号（约 1.47 GB），保留每个文件指纹，不删除源码、安装包、数据或验收证据。
- 当前安装版仍为 1.5.21；本次不修改数据库 schema 13、正式素材或沙盒数据。尚未取得真实鼠标拖动反馈，不把代码复现直接写成原生失败的唯一原因，全部 E01–E30 仍未放行。

## 1.5.21 - 2026-09-08（调度死锁修正候选，验收中，未发布）

- 解锁后保留 1.5.19 原现场诊断：16:20 提醒的到期时间正确但没有 occurrence；学习页持续加载，其他页面可响应。
- 提取原有学习邀请抑制分支后，真实学习数据库/互斥锁回归稳定超时：Rust 2021 的 `if let` 临时锁仍在分支内，重复获取同一锁导致专注结束后的调度无法继续。改为一次加锁完成评估和记录，两次邀请评估共用；失败补偿先释放学习锁再回滚。
- 新增 3 项回归覆盖抑制后继续读取、最终条件变化和事件写入失败后的重试。学习运行时 14 项、综合校验（前端 308 项及 5 项包工具测试）、Rust 工作区全量/无学习测试和两种 Tauri 生产构建通过；主库分别 333 / 241 项通过，各有 1 项原有忽略。首次测试样本使用非 UUID 导致校验失败，修正样本后重跑，失败日志保留。新安装版复测尚未开始。
- 主数据 schema 13 不变，保留 1.5.20 位置修正；不把旧安装证据转记为新版本通过，不发布稳定版。

## 1.5.20 - 2026-09-08（位置修正候选，安装版复测待完成，未发布）

- 修正拖动结束时可能先于原生移动完成、从而保存旧坐标的问题；移动确认与最终坐标读取/保存串行处理，紧接的下一次拖动从已确认位置开始。
- 移动失败不覆盖已保存位置；提前松手/取消使迟到的起始几何失效，保留快速点击和专注入口行为。
- 先在旧实现上复现错误保存，再加入 8 项组件回归；模拟缩放、连续拖动及失败恢复通过。前端 308 项及 5 项包工具测试、综合校验、Rust 工作区全量/无学习测试和两种 Tauri 生产构建通过，辅助程序 2 项边界测试及重建通过。首次综合校验因空间不足主动中止、首次无学习测试因写满失败，原日志保留，清理旧编译缓存后顺序重跑通过。
- 新安装版尚未启动或实机复测；不转用 1.5.19 的旧安装证据，不将源码回归与构建成功写成拖动持久化的实机通过。当前沙盒和其中的合成数据保持不变。
- 正式素材仍未替换，图集动态/缩放及完整 E01–E30 矩阵没有放行。

## 1.5.19 - 2026-09-08（修复候选，未发布）

- 修复真实 1.5.18 安装版中球互动结束后注视帧残留的问题：恢复业务动画时清除互动期间的鼠标注视姿态，即使后端语义修订不变，也不会用基础图集遮住工作装扮。
- 新增 4 项回归，先在旧实现上复现失败，再验证正常、过渡、疲劳三个阶段及后端先结束租约的顺序。针对性 31 项、全量前端 300 项、综合校验、Rust 工作区及无学习测试、两种生产构建均通过；磁盘空间和构建参数导致的首次失败日志保留。新安装版正常工作阶段的注视恢复针对性原生复测通过，专注 ID/期限不变、照料仅计一次；完整安装版矩阵仍未完成，旧候选的验收结果不转记为本版本通过。
- 继续验收 1.5.18 时确认疲劳循环素材部分帧存在右侧断边，桌面及既有接触表均可见。本候选未修改素材，问题仍阻止视觉验收放行。

## 1.5.18 - 2026-09-08（验收候选，未发布）

- 修复工作疲劳过渡提前进入循环的问题：80% 阶段播放进入动作后停在当前宠物自己的稳定帧，只有后端进入 100% 疲劳阶段才循环；缺装扮和关闭动画使用该包的稳定姿态。
- 修复专注期间工具互动仍显示电脑/咖啡杯及提前恢复计时标签：统一 30 秒工具期限，前端按后端租约结束，提前结束、切换形象和抢占只释放匹配的互动，不影响专注会话。
- 修复提醒完成事件晚于提醒状态清理时被忽略的问题；补水/用餐完成动作去重，换形象、其他提醒及等待用户状态可取消旧动作和迟到读取，不新增业务计数。
- 补充 17 项前端回归及 3 项后端租约回归；前端共 296 项、综合校验、Rust 全量/无学习测试和两种生产构建通过。首次综合校验曾因构建盘写满失败，保留日志，清理旧调试文件后完整重跑通过。1.5.18 安装版复测尚未完成；保留 1.5.17 的原生失败证据，不将自动回归等同安装版验收通过。

## 1.5.17 - 2026-09-08

- 修复 1.5.16 无学习安装版仍显示宠物旁 A 学习入口的问题：以 Rust 运行时能力控制入口、邀请及会话订阅，不再仅依赖统一前端包开关。
- 能力读取失败、尚未完成或窗口已卸载时不显示入口、不调用可选学习命令；完整构建未导入词表时仍可进入学习管理，已有会话仍可恢复。
- 新增 7 项能力判断回归，保留已有窗口尺寸与焦点测试；前端 279 项、综合校验、Rust 全量/无学习测试及两种生产构建通过。
- 两种断网安装版关键复测通过：无学习版清醒/水疗/动画关闭时无无效入口；完整版本可从 A 进入空词表管理并导入学习，错答反馈中关闭动画后黑板完整，三题恰好三次作答/复习，照料计数不变。其余验收矩阵尚未完成，暂不发布。

## 1.5.16 - 2026-09-08

- 修复 1.5.15 安装版验收发现的黑板裁切：普通设置保存后重新按当前提醒、学习或互动场景申请窗口尺寸，不重建业务组件。
- 设置同步与同一轮答题更新不抢走键盘焦点；新学习会话仍可取得焦点。关闭扩展场景时按最新宠物尺寸和当前缩放恢复，迟到的尺寸计算不覆盖已结束场景。
- 新增 11 项窗口尺寸/焦点/组件保留/异步失效回归；前端共 272 项、综合校验、Rust 全量/无学习测试及两种生产构建通过。
- 断网安装版已复测答题反馈/完成页开关动画后黑板保持完整；动画关闭时完整→基础→完整切换可继续答题，恰好三次作答/复习，无额外照料记录。系统减少动态、真实缩放及其余端到端项目仍待验收，暂不发布。

## 1.5.15 - 2026-09-08

- 修复真实桌面验收发现的学习启动遗留会话：提醒阻挡或展示失败后结束该次未启动会话并释放其展示占用，允许处理提醒后重试；手动学习和接受邀请共用失败补偿。
- 失败清理仅针对精确会话修订，避免中止更新后的活动会话；不删除词表、作答或历史，清理失败明确报错。获准开始学习后才结束水疗。
- 学习启动失败后刷新实际状态，提供中文原因与旧会话恢复入口；补充后端优先级/重试/部分失败及前端恢复回归。
- 1.5.14 已实机验证中文昵称保存、原生菜单和学习导入窗口称呼同步；学习启动前置故障证据完整保留。1.5.15 前端 261 项、综合校验、Rust 全量/无学习测试及两种生产构建通过，安装版验收仍须继续，暂不发布。
- 1.5.15 断网安装版已验证提醒阻挡后重试学习成功；错题 ready 反馈中完整/基础形象双向切换与三题结算正常，无重复作答。其余端到端矩阵尚未全部通过。

## 1.5.14 - 2026-09-07

- 修复真实导入验收发现的重复文件名漏检：在解压前检查原始 ZIP 中央目录，不再让解压库按名称去重掩盖非法条目；拒绝后不建立预览、不改变宠物选择。
- “我的宠物”操作失败时立即显示并聚焦错误提示，避免提示藏在滚动区域上方；不使用平滑动画，允许键盘继续操作。
- 1.5.14 断网安装版已拒绝本轮十四类异常包；完整/基础包预览与取消正常，暂存清空且全部已取证业务字段不变。前端 254 项、综合校验、Rust 全量/无学习测试及两种生产构建通过。
- 保留 1.5.13 重复文件名包漏检的失败记录；实际恢复、业务中切换、无学习安装版及辅助功能等端到端验收仍未完成，暂不发布。

## 1.5.13 - 2026-09-07

- 修复 1.5.12 安装版确认框被访问控制拒绝的问题：显式调用已注册的原生消息接口，使用“确定/取消”，仅明确返回确定才继续；不再依赖调用旧接口的浏览器确认兼容层。
- 面板使用匹配的最小消息框权限，不开放文件选择或读写；补充实际接口名、权限映射、取消、未知返回值及调用失败的回归测试。
- 1.5.13 断网安装版已验证恢复确认框正常打开，等待与取消时提醒、学习、宠物配置及其他已取证字段均不变。前端 253 项、综合校验、Rust 全量/无学习测试和两种生产构建通过；实际确认恢复及其他破坏性流程仍待实机验收，全部端到端验收未完成。

## 1.5.12 - 2026-09-07

- 修复桌面对话框组件异步确认被误判为已同意的问题：宠物包移除、提醒删除、备份恢复及全部本地数据删除均等待明确同意，取消、待决与弹窗失败不执行破坏性命令。
- 仅向管理面板开放原生确认框所需权限；等待确认期间禁用重复操作，失败显示错误，不扩大本地文件访问范围。
- 该问题来自 1.5.11 真实断网安装版验收；保留缺包备份恢复与完整性证据，但安全确认验收判失败。1.5.12 完整/无学习修复候选已构建，仍须完成安装版复测后才能放行。

## 1.5.11 - 2026-09-07

- 修复更改宠物昵称后，学习导入文件窗口仍称呼“圆圆”的遗漏；窗口打开时读取当前有效昵称，不修改词条正文、数据格式名称或产品身份。
- 补充昵称提示回归，以及 1.5.10 真实安装版的四类包导入、重复复用、独立昵称、设置保存和进程重启证据；新候选与旧证据分别记录，完整端到端验收仍进行中。

## 1.5.10 - 2026-09-07

- 修复安装版宠物右键/托盘“我的宠物”入口无响应：将子页面加入后端原生路由白名单，并覆盖有/无学习构建的回归测试。
- 新增断网沙盒端到端验收矩阵、四种能力测试包准备和受限只读取证工具；验收状态单独记录，未通过项目不计作发布完成。

## 1.5.9 - 2026-09-07

- 新增“设置 → 我的宠物”子页面及宠物菜单入口，支持每形象独立昵称、动作预览、本地成品包导入确认、切换、移除和恢复默认；默认仍为内置圆圆，不新增第三方默认宠物素材。
- 新增 `.yuanyuan-pet` V1 离线打包工具和 Rust 安全导入器：固定文件白名单、64 MiB 双限额、真实图片解码、内容地址去重、一次性暂存确认、损坏包修复及受限本地图片协议。
- 昵称集中管理并同步宠物称呼、菜单、后续通知与无障碍名称，不改写提醒正文等用户内容。普通设置及位置保存保留宠物配置；主数据库仍为 schema 13，备份恢复缺包时保留原选择和昵称。
- 切换只重建动画资源层，保留提醒、专注、学习和休息状态；缺少扩展动作时使用同宠物基础图集，关键图集失败先降为同宠物静态图，再由后端回退内置圆圆。学习答题反馈无需等待失效动画回调。
- 自动回归覆盖昵称 Unicode 一致性、晚到加载结果、备份兼容、包损坏与修复、四种动作能力组合和切换中的学习/提醒恢复。实测范围及仍待人工验证的项目见 `docs/MY_PET_UPGRADE_V1.md`。

## 1.5.8 - 2026-09-04

- 新增休息水疗、用餐、补水、热身、学习、夜间和工作七类情境装扮；工作场景包含清醒、过渡疲劳和疲劳阶段，设置可选择关闭、仅提醒或全部启用。
- Rust 稳定核心升级为表达快照 schema v2，集中仲裁提醒、用户交互、活动、专注/学习、可信任务、水疗和睡眠；前端只执行确定性的进入、循环、退出、完成和恢复动画。
- 新增仅存于内存的 5/10/20 分钟“休息一下”会话；专注、学习和手动睡眠会结束休息，提醒与短交互只临时遮挡。
- 用餐成为完整提醒类别，支持一次、间隔、每天和每周计划、历史筛选与系统通知；主数据库通过 schema 013 事务迁移保留既有提醒、occurrence、索引和外键。
- 新增 1536×3744、18×8 的透明 `scene-atlas.webp`，所有动画声明稳定帧；资源缺失、加载失败、关闭动画或关闭装扮时回退到既有动作，不影响提醒处理。
- 情境变化使用固定本地无障碍名称并仅在状态切换时播报；运行时仍保持离线，不上传提醒、任务摘要、图片或行为数据。
- 新增最终安装器绑定的离线 Windows Sandbox E2E：覆盖干净安装、真实窗口与提醒、隐藏恢复、面板拖动、备份恢复、重启持久化、默认卸载保留和显式删除；升级链路使用可追溯的当前产品 pre-013/schema 12 源码构建版本资源为 1.5.7 的测试基线，验证 12→13 原地迁移、提醒与 occurrence 逐字段保留、外键/索引、用餐提醒、覆盖安装、默认卸载、恢复旧库并回装启动 1.5.7。报告明确保留基线原始版本与版本覆盖事实，不冒充历史正式发布产物。

## 1.4.0 - 2026-08-03

- 修正发布候选身份边界：Tauri 构建目录主程序的包类型标记为 `UNK`，NSIS 实际安装主程序为 `NSS`，两者不再被当成同一文件。新增一次性静默安装—提取—卸载门，把 NSIS 实际主程序作为第五项清单产物，并将便携主程序、安装主程序和安装包三项同时纳入签名、同证书和 Defender 检查；默认冷启动也改为真实安装变体。当前提取/清理证据通过，未签名候选继续保持 NO-GO。
- 新增候选绑定的安装器转换、失败恢复、首次启动数据库恢复与卸载数据选择探测：冻结官方 v1.3.2 Setup 与安装主程序哈希，在一次性当前用户安装根执行 1.3.2→1.4.0→卸载当前版→回装1.3.2，逐阶段核对版本、字节数、SHA-256、四份许可证和正式数据目录合成哨兵，并验证快捷方式/数据/临时目录/进程零残留。默认安装路径文件转换同样通过，但当前受管账户未暴露HKCU产品注册，报告如实保持`registrationGatePassed=false`；预检新增独立`default_install_control_panel_registration`待办，不再只把这一差异留在说明文字。另以确定性损坏候选和旧主程序独占锁发现并修复NSIS可能返回0却形成“旧主程序+新卸载器/许可文件”的半升级缺陷；修复后两类失败均以非零状态中止、旧版边界精确不变。schema v2进一步把原始候选放入受限Windows Job，在观察到部分主程序写入后终止整棵进程树，证明不完整文件集、旧卸载器和数据哨兵边界，随后由未修改候选完整恢复。独立首次启动探针又在全新合成数据目录观察到非空SQLite WAL后终止1% CPU受限Job，以同一未修改候选恢复到可见窗口、架构11和完整默认数据，并保留无旁文件的规范化数据库供独立复验。真实卸载界面的数据复选框初始关闭：默认卸载保留LocalAppData与RoamingAppData合成哨兵，明确勾选后两处才被删除。当前发布预检为13项通过、14项待完成；控制面板注册、真实旧库迁移、真实掉电/系统重启、签名、安全软件和无障碍人工矩阵仍保持待办。
- 完成本地诊断卡的用户选择保存位置与独立敏感扫描：预览展示唯一JSON、预计大小和扫描v1的4项结论，Windows原生窗口只接受本机普通驱动器上的`.json`；取消不创建文件，既有目标不覆盖，临时/最终文件均复读扫描，绝对路径不回传且无应用内副本或自动上传。二次确认打开后焦点进入主操作，取消后返回原入口；状态变化提供实时与忙碌语义，卡片适配Windows强制颜色。非默认真实Tauri诊断验收在当前150% DPI下完成逻辑404.7×628.7窄窗、Enter/Tab焦点链、原生保存窗口、434字节合成导出、敏感匹配0、关键无障碍边界、隐私背景、零正式写入和受控清理；独立校验器拒绝DPI虚拟化、来源漂移、错尺寸/纯灰截图及敏感/陈旧/乐观导出。其余DPI、第二账户、满盘、安全软件与Narrator人工听读仍待完成。
- 完成默认正式程序的独立Windows测试账户新配置冷启动门：当前改为运行从 NSIS 精确提取的实际安装主程序，最新候选3次真实可见窗口P50/P95为136.4/230.1 ms，AI子进程、Application Error及数据/进程残留均为0。普通账户进程树清理由WMI改为Toolhelp原生快照并增加WebView锁释放重试；报告以SID/ProfileList证明账户目录归属并绑定采集脚本。发布清单在产物不变时保持稳定哈希，避免预检自身使候选绑定证据无条件过期。
- 新增24小时常驻、睡眠/唤醒与锁屏/解锁严格验收门：隔离运行报告绑定主程序、夹具和执行脚本，要求至少24小时墙钟、20小时活跃采样覆盖及两组真实Windows系统事件；工作集、私有内存、句柄和线程使用六段中位点斜率与首末段增长双门。独立复核器重算全部统计并拒绝来源漂移、篡改、缺失事件、乐观通过和短测冒充；真实24小时正向运行仍待完成。
- 生成确定性的生产依赖第三方许可证全文归档，并把项目许可证、素材许可、第三方摘要和逐组件许可证四份材料安装到`licenses`目录且随卸载清理；归档覆盖301个生产第三方组件、22种精确许可证表达式和11个有来源证明的回退映射，5个MPL组件提供精确源码下载地址。新增候选绑定的结构化人工复核包与签字校验器，冻结三项正式产物、八份材料、渠道/发布者/地区和全部复核决定，拒绝AI/自动化签字、候选漂移、未解决发现及只改裸布尔值的伪通过。实际具名人工许可证与素材权利复核仍保持待完成。
- 新增候选绑定的SmartScreen/第三方安全软件外部测试包、具名人工签字模板和独立校验器：冻结三项正式产物、干净机快照、Internet Zone/Mark-of-the-Web、在线信誉、完整安装生命周期及至少两款非Defender实时防护产品的零检测要求；拒绝AI测试人、旧候选、SmartScreen警告、重复产品、Defender冒充和乐观计数。当前实际外部测试签字不存在，两项门继续保持待完成。
- 新增候选绑定的签名/RFC3161协议包、无凭据具名人工签字模板和独立校验器：实时结合Windows Authenticode事实，要求三项最终产物均为Valid、同发布者/同证书、各有时间戳证书，并精确记录签名工具、执行证据、`/fd SHA256`、`/tr`和`/td SHA256`；拒绝AI操作人、SHA-1、旧式时间戳、凭据暴露、证书漂移和只改裸布尔值的伪通过。当前渠道、发布者、时间戳URL和实际签名仍待冻结。
- 新增候选绑定的无障碍人工验收包、关闭失败模板和独立校验器：严格要求最终签名候选完成四档DPI、三档文本缩放、深浅Windows高对比度、减少动态、五条全键盘、六条Narrator实际听读及安装/两种卸载数据选择矩阵；拒绝AI测试人、自动化主要结论、矩阵缺档、Narrator未实际听到、未签名候选和只改裸布尔值的伪通过。当前只生成包与模板，实际人工矩阵仍待完成。
- 新增完整升级回退候选包、具名人工签字模板和独立校验器，把六个自动子门、可写HKCU控制面板注册、真实v1.3.2数据库迁移、最终签名候选、物理掉电/硬重置、真实Windows重启和恢复旧库绑定为同一门；拒绝AI测试人、优雅退出冒充掉电、杀进程冒充系统重启、旧版打开schema v11数据库、弱签名/迁移结果及只改裸布尔值的伪通过。当前真实迁移报告与签名实际签字缺失，完整门继续待完成。
- 新增仅开发环境可见的“理一理”数据去向与重新授权原型：用户在输入正文前先选择本机或云端、查看固定数据流并明确确认；五分钟单次回执在切换去向、服务变化、过期、使用或取消后失效。AI隔离进程同时增加不保存正文的内存单次授权核心和固定Provider请求层，只允许本次用户原文进入回环适配器，令牌、Provider身份及任务/历史/文件等上下文不外发；专用结果门仅向工具层返回“事实、感受、可控、下一步”，通用模型回复和动作不能进入猫咪表达层。尚未接入真实Provider或生产入口，正式Support Box继续关闭。
- 冻结“理一理”独立本地请求协议v1 Schema并增加AI进程内可信分发器：Provider披露、明确确认、单次签发、提交、取消和全撤销与健康/关闭控制通道及任务事件管道完全分离；授权前没有正文入口，提交不能附带任务、历史、记忆、文件或工作区，响应不能返回通用模型回复。披露摘要由Rust注册表生成，适配器只在正文校验和授权消费后创建；安全双向传输和生产命令仍关闭。
- 加固“理一理”隔离进程原型：独立Windows命名管道在读取正文前核对客户端PID与进程创建时间，并限制请求和响应体积；连接、完整写入和响应读取共用3秒硬截止，静默AI不会无限挂住调用线程。32字节随机会话只经继承的标准输入送入AI进程，不出现在命令行或环境变量，畸形引导在数据库启动前拒绝。真实子进程回归证明强退后必须使用新会话才能重启；服务端取消/背压、父进程/签名认证、Tauri接线与正式监听仍保持关闭。
- 稳定核心AI监督器已接入“理一理”私密引导生命周期：每个AI启动或重启epoch生成新session并通过继承stdin写入；写入失败立即终止子进程，AI退出、启动失败、熔断、关闭和显式重启均移除父进程持有的session。session尚未用于业务请求，AI监听、Tauri入口和安装包收录继续关闭。
- 收紧“理一理”内存生命周期：协议请求、命令、响应和结果不再提供通用克隆能力；session、一次性授权令牌、用户正文、四格结果及专用JSON/管道缓冲在正常释放时主动清零。该措施只缩短普通堆残留窗口，不替代崩溃转储、换页文件和系统管道缓冲的独立取证验收。
- 修复命名管道连续投递竞态：客户端现在会在既定连接预算内等待服务端从上一条消息的实例切换到下一实例，短暂`FILE_NOT_FOUND`不再直接丢弃紧邻事件；AI确实缺失时仍按预算静默返回不可用。
- 调整任务“没成功”时的非语言反馈：圆圆只做一次短暂察觉动作，随后在任务牌仍可见时回到安静陪伴；减少动态模式直接保持安静，不再循环表现沮丧，避免把工作挫折转化为用户照顾小猫的压力。
- 修正任务动作的语义选择：长时间运行、用户主动“只陪我一会”和普通陪伴不再误用失败动作；需要用户只抬爪提示一次，可能停住、状态未知和已取消只检查或收起一次，随后保留道具并安静等待。减少动态跳过上述短暂动作。
- 新增任务失败陪伴动作的隔离 Tauri 运行验收：合成一条权威失败终态，分别锁定完整动态与减少动态，从 Windows 无障碍树确认固定语义并连续采帧；任何窗口、帧数、退出或清理异常都会生成未通过报告，正式发布程序阻止收录该验收配置。
- 完成任务失败、等待用户和疑似停滞三类提示动作的真实 Tauri 正向验收：应用可见启动前放置不可激活的中性隐私背景，避免联系表捕获桌面或其他应用；三场景完整动态均显示一次性动作并正确收束，减少动态均稳定为单帧。新增哈希绑定的人工视觉复核清单与独立校验器，拒绝缺场景、来源漂移、隐私背景未受控、视觉文件被替换或减少动态失守。
- 修复 AI 连续服务的 Windows 停机竞态：主循环收到内部管道唤醒后会先确认消息再退出，不再让关闭线程等待客户端超时；任务提交后确认和失败不确认规则保持不变。
- 加固 Windows 来源工具信任复核的本机证书读取：只在存在有效 WinTrust 状态时关闭，通过系统证书链辅助函数取得并检查首个证书项，并限制证书名称缓冲区；新增本机受管理 Codex 的 8 线程并发签名与包身份回归。该改动降低原生指针风险，但不把缺少崩溃转储的历史间歇性测试崩溃误报为已证明根因。
- 任务守望台新增用户主动的“10 分钟后再提醒 / 恢复提醒”：只暂缓圆圆对指定来源与固定状态组的主动表达，任务仍持续可见且来源工具不受修改；主库schema v11仅保存来源、状态和暂缓时间。到期失败按来源聚合一次，专注或更高优先级状态下不会提前消费，续期也不会被旧到期确认误删。
- 新增返回原任务工具的稳定核心安全门禁：只接受内置连接器的固定动作和短ASCII不透明任务标识，以2分钟一次性凭证隔离原始目标，并在点击后按当前注册表二次校验；文件、URI、Shell、脚本、路径、篡改、过期、撤销和重放全部关闭失败。真实来源处理器未验收前仅生成任务守望台降级决策，尚无正式按钮或命令。
- 收紧任务守望状态归组与证据边界：活动运行不能被另一运行静默替换，旧运行不能回流，父关系可补全但不能在同一运行中改挂；部分或未知证据的完成、失败、取消和等待用户统一降为“状态不明”，后续权威证据仍可正常确认结果。
- AI任务库升级到schema v4，仅为当前任务摘要增加固定证据等级；旧任务安全默认为未知，不从历史正文猜测可信度，迁移失败保持v3数据库不变。
- 新增默认正式主程序的新配置冷启动采样器：只允许在无既有圆圆数据的干净Windows测试账户运行，以清单哈希绑定不含实验sidecar的主程序副本，记录至少3次真实可见窗口时间并安全清理测试数据；对应证据已纳入发布预检。
- 新增 v1.3.2 数据库只读隔离迁移验收：按旧表原列逐值核对数据，覆盖生产备份恢复和强制迁移失败后的安全快照回滚；同时修复迁移 SQL 失败后事务未关闭可能令同连接回滚暴露半迁移结构的问题。补充关闭态 WAL 源库的稳定快照 CLI，以 SQLite Backup API 生成无 sidecar 单文件并复核逻辑逐值一致。GitHub 正式 v1.3.2 Portable 已完成版本、资产摘要和发布校验文件核对，但在当前隔离普通账户中未进入 `setup`、没有生成数据库，仍不把合成或诊断运行记为实机升级通过。
- 任务守望数据库的有界清理由启动后单次执行升级为低频维护：AI服务先完成双管道健康，再于5秒后首轮清理并每6小时重复；服务关闭会立即停止并回收维护线程，单轮失败不会中断任务接收或取消下一轮尝试。全新AI库从创建起启用增量回收，每轮清理后最多回收256页；既有库不会被静默全库重写。
- 新增一年等价任务保留负载验收：固定生成3,650个任务、175,200条事件和7,300个nonce，在随机临时库中运行生产清理至固定点并记录耗时、WAL、空闲页和全部保留不变量；验收程序和报告不进入正式安装包。

- 新增用户主动发起的三种基础陪伴：“只陪我一会”“陪我动一动”“先别管我”；仅使用固定猫咪动作和固定时长，不判断情绪、不调用模型、不保存原因。
- 基础陪伴会自动结束，也可随时手动结束；喝水、到期工作事项和等待用户仍可优先提醒，应用重启后不会恢复陪伴状态。
- 新增低频非语言重逢仪式：仅在白天因真实闲置自动入睡且离开至少10分钟后，圆圆用靠近和轻蹭动作迎接；夜间安静、手动唤醒、短暂离开、专注、提醒或基础陪伴会阻止触发，并与专注结束共用主动表达额度。
- 设置新增“纯动作 / 需要时显示 / 始终显示”三档道具标签；默认只在等待确认、失败、疑似停住和状态不明时显示固定中性短标签，纯动作模式不补备用文字，任何模式都不会生成猫咪对白。
- 设置页标题和说明现在与下拉框、开关建立程序化关联，安静、喝水和活动时段分别提供开始/结束名称，便于键盘和屏幕阅读器识别。
- 外部任务道具现在可打开“任务守望台”；页面只显示 Codex/Claude Code、固定状态和有界数量，不显示标题、正文、项目路径、任务标识或精确活动时间，可信状态库不存在时不显示入口。
- 面板所有按钮、输入框和选择框补充统一键盘焦点环；任务守望台适配 Windows 强制颜色，并在正式构建中排除开发用固定状态夹具。
- 新增仅通过独立编译功能启用的任务守望运行验收夹具；它使用带标记的隔离目录、合成认证事件和独立 WebView 数据，正式安装包会阻止收录该夹具。
- 新增Windows发布预检：重新核对候选哈希和安装边界，生成CycloneDX/许可证清单并采集Authenticode事实；严格发布门会阻止未签名、未完成安全软件或回退证据的候选。
- 修正发布预检待办项的说明：未完成的冷启动、渠道、签名、时间戳、安全软件、回退和许可证复核现在明确显示“尚未完成”，不再用通过态文案描述待办状态。
- 新增隔离运行基线采样器：在AI关闭配置下记录Tauri/WebView2整棵进程树的启动、CPU、内存和数据库增长，并检查正式目录零写入、受控退出与测试数据安全清理。
- 加固隔离提醒延迟采样器并冻结首个暖系统质量门：验收构建先生成生产前端并启用Tauri资源协议，应用以可见窗口启动；独占批次锁、每样本唯一标题、完整强提醒无障碍名称和时间顺序校验共同阻断并发或任务列表提前命中的假阳性。20个真实窗口样本覆盖5个调度相位各4次并全部通过，schema v2报告绑定主程序、夹具和脚本哈希；独立校验器重新计算逐样本延迟与分位值，并拒绝过期来源、相位/字段漂移和乐观篡改。少于20样本或三项P95超过冻结上限会返回失败。正式默认二进制、长期、睡眠和签名候选仍需独立复测。
- 新增“AI全部关闭”组合回归门：扫描生产网络与安装边界，运行无AI提醒核心和前端隐藏降级测试，并将通过证据绑定当前主程序及NSIS哈希后接入发布预检。
- 日志文件不可创建或不可写时不再导致主程序启动失败；圆圆会降级为本次运行不写持久日志，并继续提供提醒核心能力。
- 修复正式包窗口创建时前端请求可能早于核心状态注册、从而触发启动崩溃的竞态；核心数据库与运行状态现于窗口加载前完成准备。
- 正式宠物反馈进一步拆分为猫咪肢体动作、系统信息卡、计时卡和工具卡，简单回应不再显示猫咪对白气泡。
- 新增提醒管理页：普通提醒可查看、编辑、启停和软删除，喝水与活动系统提醒继续由设置页保护管理。
- 重复提醒支持自选星期，间隔提醒支持自定义生效时段。
- 稍后提醒扩展为 5、10、30、60 分钟，并在圆圆大提醒和今日待办中保持一致。
- 新增错过提醒策略：可选择恢复后继续提醒，或超过宽限时间后自动归入历史。
- 新增每日自动备份、手动备份、备份列表与安全恢复；自动备份保留最近 14 份，恢复前自动创建安全快照。
- 今日、设置、专注与互动改为独立加载和局部重试，单个模块故障不再清空整个面板。
- 补齐前端操作流、数据库迁移、备份恢复、提醒全生命周期与调度回归测试。

## 1.3.2 - 2026-07-27

- 修复当天没有互动记录时，喂养统计把数据库空值当作整数读取而导致面板无法加载的问题。
- 圆圆的猫粮、饮水、猫条、逗猫棒、摸摸和扔球次数统一按 Windows 本地自然日统计。
- 增加空互动记录与本地跨日边界的回归测试。

## 1.3.1 - 2026-07-27

- 修复从提醒中点击完成后喝水杯数不增加的问题，并防止重复完成重复计数。
- 今日统计改为按本地自然日刷新，跨零点后自动更新喝水杯数和待办。
- 圆圆的大提醒增加“完成 / 10 分钟后 / 跳过”直接操作，不再使用误导性的装饰勾选。
- 防止未处理的喝水提醒重复堆积；手动记录一杯仅结束最早的待处理喝水提醒。
- 活动提醒累计支持持久化，短暂离开暂停累计，真正离开十分钟后重新计算。
- 补齐浏览器演示数据交互和回归测试，便于验证提醒、喝水计数与设置保存。

本项目遵循语义化版本号。首次公开发布从 `1.3.0` 开始记录。

## 1.3.0 - 2026-07-25

- 新增长时间连续使用电脑后的活动提醒。
- 活动提醒可配置启用状态、有效时段和提醒间隔。
- 喝水与活动提醒重叠时，先处理喝水，再提醒活动。
- 新增圆圆开心蹦跳的活动专属动画和醒目提醒气泡。
- 活动完成、稍后提醒和跳过均写入历史记录。
- 完善任务历史、提醒恢复和交互收尾逻辑。
- 提供 Windows x64 安装版与便携版。
