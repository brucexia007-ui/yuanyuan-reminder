# 一段提示词制作自己的桌面宠物

普通用户不需要预先下载项目、运行命令、安装开发工具或编辑 JSON。只需把下面整段提示词复制到具备本地文件和 Coding 能力的 Kimi Code 或 WorkBuddy，替换宠物名和性格，并在同一条消息中附上宠物照片。

```text
请基于官方 GitHub 项目 https://github.com/brucexia007-ui/yuanyuan-reminder ，直接为我完成一个可安装的 Windows 桌面宠物版本。我的电脑上没有预先下载这个项目。

宠物名：<填写宠物名>
宠物性格：<填写宠物性格>
宠物照片：使用我随本消息附上的全部照片

我确认拥有这些照片的使用权，并授权它们仅用于本次桌面宠物制作。不得把原始照片、私人路径或家庭环境信息提交到 Git、公开目录、安装包或日志中。

请自主创建工作目录，从上述 GitHub 地址获取默认分支的最新完整源码，并核对仓库来源准确无误。不要让我下载项目；不要让我运行命令、安装依赖、编辑 JSON、填写 identifier、判断品种/性别或处理图片。需要系统权限时只向我请求必要授权，不要把命令交给我执行。获取源码后，先完整阅读 AGENTS.md 和 customization/PET_CUSTOMIZATION_AGENT_PROMPT.zh-CN.md，并严格遵守其中的隐私、素材、动作、验证和交付要求。

你需要自行准备项目所需的可信开发环境和依赖，并找到本消息的照片附件；如需整理输入，只能放入被 Git 忽略的 work/ 目录。然后由你在内部运行项目的 customize:auto 入口，传入附件、宠物名和性格，并根据返回的 runId/resumeCommand 持续执行和修复，直到任务完成。品种、性别和视觉特征应从照片推断；无法可靠判断的非关键字段保持 unknown/auto，不要因此打断我。

成品必须保留照片中同一只宠物的身份特征，使用标准 8×11 v2 动画图集并补齐睡眠、生活互动、学习表达、静态回退图和 Windows 图标。猫条、八方向逗猫、摸头、扔球、提醒优先级与专注安静状态必须按原版功能语义实现，不能用单张图片缩放、旋转或抖动冒充动画。

请自行执行完整验证、Rust 测试和 Windows 正式构建。发现失败就继续修复并重测；只有遇到确实无法自行获得的授权或运行环境时才向我说明阻塞。完成后只需告诉我：安装包、便携版、SHA256SUMS.txt、动作 QA 图和交付清单的位置，以及最终验收结果。
```

这段提示词同样适用于 Codex。内部自动化入口仍保留给 Coding 工具和维护者使用，普通用户无需接触。
